# 中性纹理 Floyd–Steinberg / Burkes A/B

基线为 IMG_9964 已通过的暖色连续候选。A 使用当前 PhotoBalanced 的 Floyd–Steinberg，B 使用代码中已有并已受测试覆盖的 Burkes；两者颜色目标、LUT、RGB565 输入与所有色彩修正完全相同，只改变误差传播核。

列顺序为 A/B/B/A。行 21、22、23、24 覆盖四档标准灰；16 洋红与 14 橙色用于检查换核是否造成可见色相、明暗或颗粒退步。六个裁块均从各自完整 400×600 色表渲染后裁取 88×50，设备直接显示原生码，不二次量化或抖动。

四档灰在两种模式下始终只含黑白码。内区黑/白计数如下：21 A `3609/519`、B `3615/513`；22 两者均 `2531/1597`；23 A `1471/2657`、B `1463/2665`；24 A `402/3726`、B `397/3731`。16 A/B 的白/红/蓝为 `579/1622/1927` 与 `577/1620/1931`；14 A/B 的白/黄/红为 `469/1154/2505` 与 `460/1157/2511`。这些小计数差来自有限裁块与扩散残差，目标没有改变。

两条完整帧各重复渲染且确定。A 帧 SHA-256 为 IMG_9964 基线 `42407c96d85fd2e70fba200c4485ae4339397a13c02f4ad378c68e22e1231a67`，B 为 `d0f71129301d96d340d84e6f084cf7f4e30d8371cab63e2c4b62108291cb16a4`。26,400 字节载荷 SHA-256 为 `bcc7acee5336a48f374b3db40a7ef7f7e5bb73c038a8ca3ef0f6b7806d6767c3`；ASan/UBSan 载荷校验、112,320 像素采样和独立组图一致。

实拍先核对两端 A 与两个 B 的重复一致性，再逐行判断规则斜纹、成片方向性、颗粒大小和平均明暗。若 B 只把斜纹换成另一种规则纹理，或 16/14 出现更差的色相和结构，则保留 A。只有实体图支持 B，才考虑把照片默认核切换为 Burkes 并做完整色表、渐变、首页和真实照片回归。

ESP-IDF 诊断固件已冻结为 `.cache/firmware/neutral-dither-ab-v1/paper_color.bin`：3,268,912 字节，SHA-256 `bd30461c59766b5c4d2b953da6eb46fe2eb7b275f1e31c78121ad61e46a97660`，checksum `d1`、validation hash 有效。配置只开启本诊断启动图，保留全部已通过的颜色策略。2026-09-11 已在写入前核对 Color 的 USB 序列号及芯片 MAC 均为 `44:1B:F6:C1:34:10`，只向 `/dev/cu.usbmodem83301` 的应用地址 `0x10000` 写入 3,268,912 字节；27.0 秒完成且设备端数据哈希校验通过。位于 `/dev/cu.usbmodem83401` 的 stopwatch 未打开。当前等待同屏实体 A/B 复核。

IMG_9967 已完成实体复核并选择 B。22/23 灰、16 洋红和 14 橙色的长斜线、人字形连锁均明显缩短，21/24 的平均明暗保持；四档灰没有彩色污染，16/14 没有可辨色相退步。详细判断见 `IMG_9967_REVIEW.md`。正式照片候选见 `BURKES_PHOTO_DITHER.md`。
