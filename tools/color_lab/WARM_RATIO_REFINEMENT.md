# IMG_9963 暖色红黄比例连续修正

IMG_9963 同屏诊断中，14 ORANGE 与 15 RED ORANGE 均选择 B：把红黄彩色覆盖的 1/16 从黄转到红。14 从金黄/黄褐回到橙色方向；15 黄色高亮点减少且更接近深红橙。13/09/12/16 控制一致。

正式实现 `refine_warm_red_yellow_ratio()` 只在红黄目标扇区执行。它按源色的红绿相对色相和饱和度平滑进入，并在红或黄颜料趋近零时平滑退出；最大转移量为当前红黄总覆盖的 6.25%。白权重、黑覆盖及红黄总覆盖保持，13 暖黄、纯红、近中性和其他色域的标准目标不变。

全 65,536 个 RGB565 目标中 4,988 个红黄扇区目标改变。191,488 条 RGB565 相邻边中，没有新增旧步长不超过 30 而新步长超过 30 的边；全域最大目标步长保持 `59.78741637042029`，候选最大目标位移为 `16.09068538782609`。报告位于 `artifacts/color_calibration/warm-ratio-refinement-comparison.json`。

14 的内区黑/白/黄/红从 `0/468/1385/2275` 变为 `0/469/1154/2505`；15 从 `0/0/777/3351` 变为 `0/0/518/3610`，均精确复现同屏 B 的原生码计数。其余 22 个标准色块目标不变。完整帧中 12、16、20 分别仅因扩散残差相位产生 1–3 个码变化，需在完整表实拍中复核；候选帧 SHA-256 为 `42407c96d85fd2e70fba200c4485ae4339397a13c02f4ad378c68e22e1231a67`。

局部权重、保护色、颜料顶点与细邻域 ASan/UBSan 检查通过。两种照片扩散器、全 RGB565 输入、原生量化单元和真实首页适配器的 ASan/UBSan 回归通过；关闭新开关的深紫基线回归也通过。

完整 24 色表固件 `.cache/firmware/warm-ratio-chart-candidate/paper_color.bin` 为 3,263,344 字节，SHA-256 `c364d9e989abb218fc202ebf171473588bdc1584e307ee24d0f26a5fef314487`，checksum `89` 和 validation hash 有效。配置开启正式暖色修正与完整色表，关闭全部 A/B/C 启动诊断。

烧录前重新核对 USB 序列号、位置 8-3.3 与同连接芯片 MAC，均为 Color 的 `44:1B:F6:C1:34:10`。只向 `/dev/cu.usbmodem83301` 的应用 `0x10000` 写入 3,263,344 字节，用时 27.0 秒，设备端哈希校验通过、退出码 0，随后请求 RTS 重启。stopwatch `/dev/cu.usbmodem83401` 未打开，引导、分区表、照片和设置存储均未写入。当前待完整色表物理验收。
