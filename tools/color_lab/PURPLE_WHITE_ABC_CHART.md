# 09 深紫白量 A/B/C 对照

基线为 IMG_9960 所用的绿系连续候选。09 深紫同时可见亮点和暗点，本图只改变其目标白量，不改变正式照片算法，也不同时调整红蓝比例。

四列为 A/B/C/A，两端 A 用于识别位置和照明差异。每个裁块都由真实 PhotoBalanced 实现先渲染完整 400×600 色表，再从完整扩散上下文裁出 88×50 原生码；设备直接显示原生码，不二次量化或抖动。

| 行 | A | B | C |
|---|---|---|---|
| 09 PURPLE WHITE | 当前白目标 | 去掉一半白目标 | 去掉全部白目标 |
| 18/17/16/12/10 | 当前基线控制 | 与 A 逐字节相同 | 与 A 逐字节相同 |

09 内区实际计数：A 为黑 140、白 218、红 1572、蓝 2198；B 为黑 142、白 102、红 1622、蓝 2262；C 为黑 159、白 0、红 1652、蓝 2317。目标变换保持黑权重与红蓝比例，实际黑码的少量变化来自误差扩散。B/C 都可能改变亮度和点阵，不能把“白点更少”直接等同于“颜色更准确”。

临时函数只识别 RGB565 展开后的 09 输入 `(99,48,156)`；`ESP_PLATFORM` 下直接编译失败，不能进入设备照片策略。三个模式均完整渲染两次且结果确定，A 完整帧 SHA-256 为 IMG_9960 基线 `7943d33235ac6f12fb74d762343af215adab36ff7cc928ad9f5e7e4422dbb7d6`。五个控制行的 A/B/C 裁块逐字节相同。

载荷 39,600 字节，SHA-256 `1a76b2956b7d7c3bc6ec94c541f9dfcd9e25ab075755e5a9ad4496cfe225aebd`。ASan/UBSan 采样器覆盖合法整帧、空/短载荷、非法半字节与越界，112,320 个采样像素与独立组图一致。PNG 只是布局和名义颜料预览，不模拟物理面板。

实拍时先比较两端 A 及五行控制，确认照明和位置影响；再只看第一行。B 的判断重点是亮点是否减少、整体是否仍保持深紫亮度及红蓝方向；C 用于显示去白边界是否明显过暗或增粗网纹。允许保留 A。

配置为 `tools/color_lab/purple-white-abc-sdkconfig.defaults`。方向明确后，才设计对一般紫色连续衰减的正式候选，并做全 RGB565、渐变、完整色表和首页回归。

ESP-IDF 构建通过；应用 3,280,032 字节，SHA-256 `987030ecebf6e25132b25f2539171d9a80d651b183e6273af3e1b2b14c205acf`，checksum `cf` 和 validation hash 有效。缓存位于 `.cache/firmware/purple-white-abc-v1/`，包含应用、实际 sdkconfig、镜像检查和清单。

用户切换下载模式后，USB 序列号、位置 8-3.3 及同连接芯片 MAC 均核对为 `44:1B:F6:C1:34:10`。仅向 Color `/dev/cu.usbmodem83301` 的应用地址 `0x10000` 写入 3,280,032 字节，写入 27.1 秒，`Hash of data verified`，退出码 0；RTS 重启已请求。stopwatch 端口未打开，照片和设置分区未写入。当前待松开 Boot、必要时单按 Reset，并拍摄保持全图可见的同屏 A/B/C。
