# 深紫白量连续修正候选

IMG_9961 同屏选择 09 行 B：把当前白目标减半。正式候选用连续的色相、饱和度和源亮度窗口推广该方向，最多将当前白权重的 50% 转入既有红蓝混合；红蓝比例、总覆盖和目标黑量保持。窗口在 09 的 RGB565 展开输入 `(99,48,156)` 达到完整强度，在更暗的 18、较亮且偏红的 17、洋红、近中性和其他色相处归零。

全 65,536 个 RGB565 输入中 698 个红蓝扇区目标改变，输入展开范围为 R 66–173、G 8–130、B 123–206，并同时受色相、饱和度和亮度窗口约束。191,488 条相邻边中没有新增旧值不超过 30 而新值超过 30 的边；全域最大目标步长保持 `59.78741637042029`，最大候选目标变化为 `23.870582051973514`。

09 目标白/红/蓝权重从约 `0.05822/0.37634/0.52585` 变为 `0.02927/0.38858/0.54254`。完整色表内区计数由黑/白/红/蓝 `140/218/1572/2198` 变为 `142/102/1622/2262`，与 IMG_9961 的 B 裁块完全一致。其余 23 个标准色块的预扩散目标全部不变。

完整帧有 19,471 个像素码变化；除 09 外，10/12/14/15/16/18/20/23 的总计数各变化 1–3 个码，来自 09 后续 Floyd–Steinberg 残差相位传播，其各自源色目标不变。完整实拍仍需逐项确认这些保护色没有可见退步。

局部函数的权重、顶点、保护色和细粒度邻域在 ASan/UBSan 下通过，最大 ±0.001 源扰动输出差为 `0.0025865`。真实照片管线的两种扩散器、RGB565 全单元/原生重建边界和真实首页显示适配器均在 ASan/UBSan 下通过。数值报告为 `artifacts/color_calibration/deep-purple-refinement-comparison.json`，复现入口为 `tools/color_lab/compare_deep_purple_refinement.py` 和 `tools/color_lab/run_deep_purple_tests.py`。

功能开关 `PAPERCOLOR_DEEP_PURPLE_WHITE_REFINEMENT` 默认关闭。完整色表候选配置为 `tools/color_lab/deep-purple-chart-sdkconfig.defaults`。通过面板完整表复核后才保留；颜色阶段完成后再进入人像局部优化。

ESP-IDF 完整色表构建通过；应用 3,262,400 字节，SHA-256 `fa1a9d5efd347a0632135a7118269735db731fc85ed415fecb092ddf8a93b0af`，checksum `74` 和 validation hash 有效。候选帧 SHA-256 为 `72db7bef3746af85f7f8d647899bd4f610351d5d21eaaf8ce69e4d6f08477cfc`。缓存位于 `.cache/firmware/deep-purple-chart-candidate/`。

用户切换下载模式后，USB 序列号、位置 8-3.3 及同连接芯片 MAC 均核对为 `44:1B:F6:C1:34:10`。仅向 Color `/dev/cu.usbmodem83301` 的应用地址 `0x10000` 写入 3,262,400 字节，写入 26.9 秒，`Hash of data verified`，退出码 0；RTS 重启已请求。stopwatch 端口未打开，照片和设置分区未写入。当前待松开 Boot、必要时单按 Reset，并实拍完整 24 色表。
