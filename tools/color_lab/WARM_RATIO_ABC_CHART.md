# 14/15 暖色红黄比例 A/B/C

基线为 IMG_9962 已通过的深紫连续候选。14 橙色仍偏金黄/黄褐且织纹强，15 红橙有高反差黄色点。本图固定白量与总彩色覆盖，只测试红黄比例，避免把色相、亮度和扩散核同时改变。

四列为 A/B/C/A，两端 A 用于识别位置和照明差异。所有裁块均由真实 PhotoBalanced 实现完整渲染 400×600 色表后裁出 88×50 原生码，设备不再量化或抖动。

| 行 | A | B | C |
|---|---|---|---|
| 14 ORANGE | 当前红占彩色 62.44% | 红占比 +6.25 个百分点 | 红占比 −6.25 个百分点 |
| 15 RED ORANGE | 当前红占彩色 80.95% | 红占比 +6.25 个百分点 | 红占比 −6.25 个百分点 |
| 13/09/12/16 | 当前基线控制 | 与 A 逐字节相同 | 与 A 逐字节相同 |

14 内区 A/B/C 的白码为 468/469/472，Q4 和扩散造成 1–4 个码差，但诊断目标白权重固定；黄码 1385/1154/1605，红码 2275/2505/2051。15 三版白码均为零，黄码 777/518/1043，红码 3351/3610/3085。B/C 都保持红黄彩色总权重。

临时函数只识别 RGB565 展开的 14 `(255,101,49)` 和 15 `(206,48,0)`，`ESP_PLATFORM` 下直接编译失败，不能进入设备照片策略。三个模式均完整渲染两次且确定，A 帧 SHA-256 为 IMG_9962 基线 `72db7bef3746af85f7f8d647899bd4f610351d5d21eaaf8ce69e4d6f08477cfc`。四个控制行三种裁块逐字节相同。

载荷 39,600 字节，SHA-256 `314b7a066e8eb794bc33bf1173b00416df3a86b439a997efd0c4b4a1d9f1c57a`。ASan/UBSan 采样器覆盖合法整帧、空/短载荷、非法半字节和越界，112,320 个采样像素与独立组图一致。PNG 仅为布局和名义颜料预览，不模拟物理面板。

ESP-IDF 固件已构建并冻结为 `.cache/firmware/warm-ratio-abc-v1/paper_color.bin`：3,281,280 字节，SHA-256 `edbdf6bd09356c7e26a18d13d82181ccbf5f495513fc61bfc018bd7941d46fa4`，checksum `1a`、validation hash 均有效。配置只开启本轮暖色比例启动图；其他启动诊断均关闭，已通过的绿系和深紫正式修正保留。

烧录前重新核对 USB 序列号、位置 8-3.3 与同连接芯片 MAC，均为 Color 的 `44:1B:F6:C1:34:10`。只向 `/dev/cu.usbmodem83301` 的应用 `0x10000` 写入 3,281,280 字节，用时 27.1 秒，设备端哈希校验通过、退出码 0，随后请求 RTS 重启。stopwatch `/dev/cu.usbmodem83401` 未打开，引导、分区表、照片和设置存储均未写入。

实拍先核对两端 A 与四行控制，再分别判断 14、15。14 重点看 B 是否从金黄/黄褐转向橙色且不过暗，C 是否只变亮偏黄；15 重点看 B 减少黄色点后是否仍保留红橙感，C 是否更亮、更接近源红橙。14 和 15 可选不同方向，也允许保留 A。方向明确后再设计连续正式候选并做全域与完整表回归。
