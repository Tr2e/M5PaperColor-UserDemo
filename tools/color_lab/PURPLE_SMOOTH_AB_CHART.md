# 紫色启用阈值平滑：离线候选与同屏 A/B（2026-09-10）

## 状态与目的

IMG_9949 已确认回退恢复。以 IMG_9944 暖色旁路版为 A 基线；完全去除紫色补偿的 IMG_9947 继续否决。本轮仅平滑原 `R/B=1/2` 启用边界，不改变原增红上限。候选数值检查通过，**IMG_9951同屏阶段复核通过、暂保留；完整24色回归待做，不能称为颜色还原完成**。

`CONFIG_PAPERCOLOR_PURPLE_COMPENSATION_SMOOTH` 默认关闭，与旧紫色旁路互斥。仅当红蓝候选域且 B>R 时：R/B<=2/5 不补偿，>=5/8 保持旧整数增红/减蓝，中间用 smoothstep 衰减旧增量。区间宽度是数值连续性实验参数，不是实测色域拟合。

首次窄区间 0.4..0.5 虽保持色表不变，却新增 324 条超过30的 RGB565 邻接目标跳变，因此否决、未烧录。代码/报告保存在 `.cache/experiments/purple-smooth-narrow-rejected/`，没有把失败草稿当成候选。

## 离线证据

运行 `python3 tools/color_lab/compare_purple_smooth.py`，报告 `artifacts/color_calibration/purple-smooth-comparison.json`：

- 扫描全部65,536个RGB565目标及191,488条相邻边；过渡区2,198个输入中1,958个目标变化，区间外目标不变。
- 全部24色色表240,000像素逐字节一致，两版 SHA-256 均为 `effced8df5883bd2e379812eee61f1215388757059c7c9aa9d8a4587a2c178ee`。09/17/18的红蓝黑白量、10/19边缘均保留；这证明保护项未退步，**不证明这些色块得到进一步改善**。
- 同一候选域内，跨旧启用边界的最大目标距离68.30→28.00；全域68.30→59.79；新增跨越30的相邻目标边为0。
- 过渡区接触的边（包括候选域切换）仍可达56.90，青蓝旧分支仍有59.79。没有消除全部映射不连续，也没有本轮修改青蓝支路。
- 16组均匀色、24条渐变中的20条逐字节不变，包括浅紫到深紫、纯蓝到白、全部暖色和灰阶保护渐变。
- 4条变化渐变的**实际抖动单列诊断没有全面改善**：R阈值最大相邻列124.90→124.90，B阈值70.43→72.51，蓝到洋红70.32→70.32，红蓝边74.46→59.69。原小于6变为大于12的新单列跳变分别8/12/18/2处。九列均值包络差约20.72/21.10/17.23/16.03。报告保留原始列值，不将均值平滑等同于颗粒/条纹通过。

这些数值来自名义原生RGB码，不是实体色差，手机照片RGB未参与拟合。

## 回归测试

- 旧版与平滑版均运行完整主机测试，ASan/UBSan通过；包括两个误差扩散滤波器、五种邻色、精确原色和双色边界。
- `test_purple_smooth_targets.cpp` 检查完整启用区间、两个端点、旧1/2位置、原生蓝和紫色保护；平滑版通过，旧版在跨阈值距离断言失败，证明测试能够检出旧问题。
- `test_warm_targets.cpp` 在平滑版通过；暖色阈值旁路与青蓝旧补偿保持。
- 原RGB888 `(90,0,165)` 属于本轮改变的区间，旧补偿将它推到红蓝边，平滑后目标是 `(127.9375,38.0625,151.6875)`，约4.7%黑、35.8%红、59.5%蓝。测试显式检查这个新比例（黑3..6%、红34..37%、白<1%），不将其误称为边缘纯色；旧版原39..42%红且只能红蓝的断言仍在。真实红蓝边控制 `(180,0,165)` 及青蓝控制保持严格无异色点检查。
- 新原生码采样器做完整帧独立合成比对、非法高/低半字节、空指针/短载荷/越界测试，ASan/UBSan通过。
- 运行实际 `main/CMakeLists.txt` 的独立文件夹夹具：正确输入通过；修改载荷一个字节被哈希检查拒绝。

## 实测图

`python3 tools/color_lab/generate_purple_smooth_ab_chart.py` 生成 `purple-smooth-ab-v1.{bin,json,sha256,png}`。

列顺序 **A / B / B / A**。A是旧硬阈值，B是平滑启用。重复列用于识别光照和位置差异。最上方六格是原生色锚点。

| 行 | 内容 | 来源 |
|---|---|---|
| 1 | R=70→140，G=50、B=200 | 单独诊断源图，替换原13内区为86×48渐变 |
| 2 | B=160→255，R=100、G=50 | 单独诊断源图，替换原14内区为86×48渐变 |
| 3 | 09紫色 | 未改完整原色表输出 |
| 4 | 17浅紫 | 未改完整原色表输出 |
| 5 | 18深紫 | 未改完整原色表输出 |
| 6 | 10蓝青边缘 | 未改完整原色表输出 |

两份输入都经真实C++照片流水线、RGB565转换、完整400×600源帧扩散后再裁剪。控制行始终从未改原图裁剪；渐变行从单独诊断帧裁剪，**不是伪称原色表自带渐变**。88×50原生裁剪含边框，不缩放、不重抖动；12个独立裁剪4bpp合计26,400字节，复制为24个屏幕格。

载荷SHA-256 `dbdf49ec775d72ced2680071413c6226e2a531fb4026a3c59f698a257562ea9e`。采样器112,320个原生像素与独立组图一致。素材/实际代码哈希写入清单，构建拒绝过期输入；不会覆盖已有暖色/减白诊断历史素材。PNG只作名义预览。

验收逐项顺序：先看09/17/18是否四列一致、无灰蓝/蓝黑退步；再看10是否保持无规则边缘点列；最后比较前两行过渡突变、黑白颗粒、明暗台阶和色相。A/B重复差异若受光照影响，应先排除拍摄位置。图中不包含完整24色，不能代替后续完整表回归。

## 构建与安全烧录

配置 `tools/color_lab/purple-smooth-ab-chart-sdkconfig.defaults`，构建目录 `/private/tmp/papercolor-purple-smooth-ab-build`。图表启动开关默认关闭且与其他诊断互斥。应用已构建成功并缓存到 `.cache/firmware/purple-smooth-ab-v1/`，完整构建信息见其manifest。

只允许下载身份与同连接MAC均为 `44:1B:F6:C1:34:10` 的PaperColor，应用地址 `0x10000`。stopwatch `44:1B:F6:C1:8A:00` 不打开。运行模式USB `303A:4002` 不能误判为下载；当前仍为 IMG_9949 对应应用，须等用户将Color切入下载模式。照片、设置分区不写入。

应用3,261,680字节，SHA-256 `ed7dfdbe170c73eebd71e1bdb379e0534e37178ed8990ebf046f4a558489bc3e`，checksum62和validation hash有效。与IMG_9949配置相比仅开启平滑与新A/B启动、关闭完整色表启动。尚未烧录。回归复现入口：`python3 tools/color_lab/run_purple_smooth_tests.py`。

### 烧录完成（2026-09-10）

用户切换后，USB序列号和同连接芯片MAC均核对为 `44:1B:F6:C1:34:10`。仅向PaperColor `/dev/cu.usbmodem83301` 应用0x10000写入3,261,680字节，写入27.0秒，哈希校验成功、退出码0，RTS重启已请求。stopwatch未打开，照片/配置分区未写入。上方“尚未烧录”描述为构建阶段记录；当前已烧录，仍待松开Boot、Reset及完整A/B实拍验收。缓存manifest和flash.log为最新烧录记录。

### IMG_9951实拍复核

详见 `IMG_9951_REVIEW.md`。B的两条过渡较缓，第二条更明显；保护行09/17/18/10未见明显退步。第一条仍有宽过渡带和颗粒变化，暂保留并进入完整24色回归。上述构建/烧录前状态为历史记录；当前已完成同屏实拍复核，完整表候选另行准备。

### 完整色表候选已准备

配置 `tools/color_lab/purple-smooth-chart-sdkconfig.defaults`，构建 `/private/tmp/papercolor-purple-smooth-chart-build`，缓存 `.cache/firmware/purple-smooth-chart-candidate/`。应用3,255,696字节，SHA-256 `457106f2e2dd3b39498cc4e0047c09a9ffb3ec1e07f09496bf52e1d683997c93`，checksum dd/validation hash有效，尚未烧录。与A/B配置仅交换启动页面，与IMG_9949配置仅增加紫色平滑开关。算法和已有主机测试输入哈希未变，完整帧仍应保持IMG_9949；此次完整表用于动态照片路径与全部24项回归。

### 完整色表烧录完成（2026-09-10）

用户切换后再次核对USB及同连接MAC `44:1B:F6:C1:34:10`，仅向PaperColor应用0x10000写入完整表镜像 `457106f2…`（3,255,696字节），26.9秒，哈希校验成功、退出码0。stopwatch未打开，照片/设置分区未写入。当前已由A/B切到完整表固件，待松开Boot、Reset及实拍/运行指标回归；此前“尚未烧录”为构建阶段记录。

### IMG_9952完整表回归通过

详见 `IMG_9952_REVIEW.md`。09/17/18、10/19及其他色块未见明显新增退步，动态照片路径success=1、工作区14,496字节。保留平滑改动作为实验基线，整体颜色尚未校准；下一轮优先16/20混色目标和白量，当前无需重复烧录或复拍同版。上方各阶段待验收状态保留为历史记录。
