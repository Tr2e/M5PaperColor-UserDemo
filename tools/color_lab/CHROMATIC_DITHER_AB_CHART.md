# 彩色纹理 Floyd–Steinberg / Burkes A/B

IMG_9968 完整表中，20 青色疑似在 Burkes 下出现更明显的人字纹，11 黄绿有较弱的同类疑点。由于 IMG_9964 与 IMG_9968 的拍摄条件不同，本图在同一屏幕上固定颜色目标、LUT、RGB565 输入、裁块位置和刷新，只改变扩散核。

列顺序为 A/B/B/A。九行覆盖 09/10/11/12/13/15/17/18/20，即中性 A/B 没有覆盖的全部非原生彩色色块。每个 88×32 裁块来自对应的完整 400×600 色表渲染，设备直接显示原生码，不做二次量化或抖动。先核对两端 A 和两列 B 的重复一致性，再优先比较 20 青色与 11 黄绿的长链、方向性和颗粒，随后检查其余各行的纹理、色相和明暗。

两个完整帧均重复渲染且确定。A 帧 SHA-256 为 `42407c96d85fd2e70fba200c4485ae4339397a13c02f4ad378c68e22e1231a67`，B 为 `d0f71129301d96d340d84e6f084cf7f4e30d8371cab63e2c4b62108291cb16a4`。25,344 字节载荷 SHA-256 为 `3e5c846d7fe0f87c47cf339f1d9f2ef9a4a5d906543572e936182cce4623433f`，ASan/UBSan 载荷读取与独立组图一致。各行颜料计数见 `artifacts/color_calibration/chromatic-dither-ab-v1.json`；A/B 仅有有限裁块的扩散相位计数差，没有改变颜色目标。

若 B 在 20 或其他色块形成更强的规则纹理，则暂不全局接受 Burkes。此时先评估统一的第三种扩散核，不在单帧内按源颜色硬切不同核，以免在色域边界产生新的误差传播断层。

ESP-IDF 诊断固件已冻结为 `.cache/firmware/chromatic-dither-ab-v1/paper_color.bin`：3,267,872 字节，SHA-256 `2976f8dbf7b2e34ac1115ba93dc2526271483874b6f2e5b6c4f352cbd5a111a0`，checksum `cd`、validation hash `d33e8b5f1da9d678618713d0593689fc320bf80437ed7445f68d14622bd6df2e` 有效。配置关闭生产 Burkes 选择，只开启本轮预渲染诊断页，保留全部颜色目标策略。2026-09-11 已核对 Color 的 USB 序列号及芯片 MAC `44:1B:F6:C1:34:10`，只向 `/dev/cu.usbmodem83301` 的应用地址 `0x10000` 写入该文件；3,267,872 字节在 27.2 秒内完成，设备端数据哈希校验通过。烧录时未枚举到 stopwatch 端口，且未打开其他串口。当前等待同屏实体复核。
