# PaperColor 校色接续 TODO（2026-09-08 暂停）

## 阶段提交（2026-09-11）

用户要求阶段性本地commit。本检查点保存截至IMG_9958的校色实现、默认关闭实验开关、
测试/诊断工具及实拍复核证据；整体校色未完成，后续优先11/12。说明见
`tools/color_lab/CHECKPOINT_9958.md`。缓存固件和手机HEIC不纳入Git，本轮不push。

## 最新进度：IMG_9958量化修复及完整回归通过，下一项11/12绿系

- 全24项见 `tools/color_lab/IMG_9958_REVIEW.md`。03/04/06填充干净，06零星暗点不再明显；
  结合匹配源码主机异色码归零，量化修复保留。目视不代表逐物理像素完全无瑕疵，
  孤立细浅痕不一并归因软件改善。11/12/16少量相位变化未见明确新增退步。
- 09/17/18保护保持，10/19规则左缘点列未复发，20青色、暖色及灰阶未见明显新退步。
  整体颜色、洋红、橙色、黄绿和网纹仍未合格，当前仅保留为下一实验基线。
- 当前应用0d073d1b…，缓存二进制与96项当前输入哈希再次核对一致；manifest已记录验收。
  既有主机帧61b7a318…未重复渲染，照片未做绝对色度拟合。
- 仅打开新鲜匹配的Color运行口，reference_chart success=1、photo-balanced，
  prepare1.155403 s、total17.630811 s、render0.417970 s、panel17.212836 s，
  workspace14,496字节；记录 `artifacts/color_calibration/IMG_9958-native565-chart-usb.txt`。
  stopwatch下载口未打开，未再次烧录，照片/设置未动。
- 下一项独立检查11黄绿和12青绿，拆分颜料比例、白色量与扩散纹理成因；
  不把12亮点套成原生量化缺陷，不扩大相邻色吸附，不改RGB888。14/15、16及灰阶后续处理。
- 本轮未改算法、未构建下一候选、未提交/推送；无需重复拍同版色表。颜色后再做人像。

## 上轮进度：原生RGB565重建完整表已烧录，待实拍验收（2026-09-11）

- 用户“好了”后重新核对Color下载USB序列号与同连接芯片MAC，均为44:1B:F6:C1:34:10。
  另一台stopwatch为44:1B:F6:C1:8A:00，未打开其端口。
- 已写 `.cache/firmware/native565-chart-candidate/paper_color.bin`，3,258,096字节，
  SHA-256 `0d073d1ba28e48931dcea5b73cc4efe37abd7a55373346c45b005b57728d565f`，
  仅应用0x10000，写入27.0秒，Hash of data verified，退出码0；manifest/flash.log已更新。
  照片/设置分区未写入，RTS重启已请求；需松开Boot、必要时单按Reset。
- 烧录前镜像、配置和96项匹配输入快照哈希核对通过；本轮未改算法、未重建、未提交/推送。
- 下一张完整24色表先查03/04/06杂色点是否改善，再查11/12/16扩散纹理变化和
  09/10/17/18/19/20、暖色及灰阶保护；读取动态照片路径指标。尚未物理验收通过。
  软件不保证消除原生色块上的孤立浅曲线/物理痕迹，不据此扩大重建范围。
- 主机候选完整帧61b7a318…，回退IMG_9957的33680275…完整表保留。颜色阶段后做人像。

## 上轮进度：原生RGB565量化重建候选已构建，待Color下载模式

- 按用户“继续推进”独立修复03/04/06。新默认关闭NATIVE_565_RECONSTRUCTION只在照片
  Swap565Source重建四个原生量化单元；相邻单元、RGB888和UI最近色路径不改。
  同一565单元原始颜色不可区分，不声称恢复任意RGB888。详见 `tools/color_lab/NATIVE565_RECONSTRUCTION.md`。
- 全65,536编码仅四个采样/目标改变，重新打包仍属原单元；RGB888目标对照不变。
  191,488邻接边无新增>30，受影响最大9.0011→10.0010，全域59.7874保持。
- 03/04/06各4128内区成为纯黄/红/绿，白9/黑30/黑16归零；18个完整色块裁剪保持。
  11/12/16因扩散上下文产生少量计数/纹理变化，目标未改；09/10/17/18/19/20和灰阶保持。
  完整候选帧61b7a318…，共4427像素变化；20保护均匀色不变，12条原生附近渐变已记录。
- 基线/候选ASan/UBSan、四原生块完整边界/五周边色/两扩散器、所有UI最近色通过；
  基线负对照检出错误代表值。首页旧测试的跨格式相等假设因此次格式重建不再成立，
  已补成各格式对同格式真实浏览器比较，开关关闭仍要求跨格式相等；两套配置均通过。
- 完整色表固件已构建，`.cache/firmware/native565-chart-candidate/paper_color.bin`，
  3,258,096字节，SHA-256 `0d073d1ba28e48931dcea5b73cc4efe37abd7a55373346c45b005b57728d565f`，
  checksum64、validation hash有效；保存配置、96项输入快照、测试/构建记录。
  相对IMG_9957实际配置仅开启一个重建开关，青色/紫色/暖色系数未变。
- **尚未烧录、尚未物理验收。** 设备最后验证应用仍为33680275…完整表，可作回退。
  待Color进入下载模式，重新核对USB与同连接MAC44:1B:F6:C1:34:10，仅写应用0x10000。
  stopwatch不打开，照片/设置不写入；下一张先验03/04/06，再查11/12/16及保护色/灰阶。
- 本轮不提交/推送，不进入人像；孤立物理浅痕不冒称此软件修复能消除。

## 上轮进度：IMG_9957完整回归通过，保留青色平滑实验基线

- 全24项见 `tools/color_lab/IMG_9957_REVIEW.md`。20未见整表新增退步，结合IMG_9955/9956
  同屏少绿方向保留青色平滑；09/16/17/18保持，10/19规则左缘点列未复发，
  暖色、其他绿系及灰阶未见明显新增退步。24轻微扩散相位变化未见明显异常。
  这不是整体色彩还原通过；偏灰、洋红、橙色、黄绿及网纹仍有明显差距。
- 新鲜核对PaperColor运行口后，reference_chart success=1，photo-balanced，
  prepare1.098379 s、total17.561380 s、render0.418168 s、panel17.143207 s，
  workspace14,496字节。原始记录 `artifacts/color_calibration/IMG_9957-cyan-ratio-chart-usb.txt`。
  stopwatch下载口未打开，未重复烧录或改动照片/设置。
- 当前应用仍为33680275…完整表，缓存二进制与90项当前输入哈希核对一致；
  manifest记录完整表阶段回归通过。主机预期帧c675e6c1…为已验证报告，本轮未重新渲染。
- 下一项独立检查03/04/06近原生RGB565量化：展开值偏离原生顶点，既有白9/黑30/黑16
  并非新边缘缺陷。先查量化单元、相邻颜色连续性、RGB888路径，不扩大任意近色吸附。
  之后继续11/12、14/15、16及灰阶纹理；已有青色渐变白点变化带继续待办。
- 本轮未改算法、未构建下一候选、未提交/推送；不再要求同版色表复拍。
  青色强度先保持，首页修复保留，颜色阶段后再做人像。

## 上轮进度：青色平滑完整24色色表已烧录，待实拍回归（2026-09-11）

- 用户“好了”后，重新枚举确认两台下载设备：Color序列号44:1B:F6:C1:34:10，
  stopwatch序列号44:1B:F6:C1:8A:00。仅打开Color，并核对同连接芯片MAC一致。
- 已写 `.cache/firmware/cyan-ratio-chart-candidate/paper_color.bin`，3,258,048字节，
  SHA-256 `33680275e7c88a1e575631b3f79c0faef9426f9db059ec6d4116ac68907d8dc7`，
  仅地址0x10000，写入26.8秒，Hash of data verified，退出码0。manifest/flash.log已记录。
  stopwatch未打开，照片/设置分区未写入，已请求RTS重启；需松开Boot，必要时单按Reset。
- 烧录前核对镜像、配置和90项源码/素材快照哈希通过。本轮未改算法、未重建、未提交/推送。
- 下一张应为完整24色色表；先检查20青色与09/16/17/18、10/19保护，再逐项检查其他
  绿系、暖色、近原生及灰阶，特别关注24的轻微扩散相位变化；取动态照片路径运行指标。
  算法等同IMG_9956的B，主机预期完整帧c675e6c1…；尚未完成全表物理验收。
- 既有A/B938fc570…与回退首页29e7cb90…均保留。颜色优先，人像局部优化后做。

## 上轮进度：IMG_9956同屏暂通过，完整24色回归固件已构建待烧录

- 逐项见 `tools/color_lab/IMG_9956_REVIEW.md`。B青色偏绿减轻，16/17保护保持，
  10规则左缘点列未复发；两条渐变未见相对A明确新增硬分界，但白点变化带、
  曲折网纹和灰暗感仍在。本次只允许进入完整回归，不宣称整体颜色/纹理已经合格。
- USB唯一筛选PaperColor运行口303A:4002、产品及位置8-3.3，图表success=1，
  total17.580446 s、render0.012865 s、panel17.567577 s；原始记录
  `artifacts/color_calibration/IMG_9956-cyan-ratio-ab-usb.txt`。stopwatch下载口未打开。
- 保持IMG_9956的B算法和系数；实际SDKCONFIG只关闭CYAN_RATIO_AB_CHART_ON_BOOT、
  开启REFERENCE_CHART_ON_BOOT，87项原输入哈希一致，没有叠加新校色或重复大批主机测试。
- 完整表候选已成功构建：`.cache/firmware/cyan-ratio-chart-candidate/paper_color.bin`，
  3,258,048字节，SHA-256 `33680275e7c88a1e575631b3f79c0faef9426f9db059ec6d4116ac68907d8dc7`，
  checksum7f、validation hash有效；保存配置、90项源码/素材/复核快照和构建验证记录。
- 主机完整表预期仍为c675e6c1…；下一张检查20少绿方向以及09/16/17/18、10/19保护，
  再逐项检查暖色、其他绿系及完整灰阶，特别核对24小幅扩散相位变化是否可见。
  完整图运行动态PhotoBalanced路径，需要同时取运行指标，本次A/B耗时不冒充照片性能。
- **尚未烧录完整表。** 当前设备最后验证应用仍为938fc570…A/B图。
  待用户切Color下载模式，重新核对USB和同连接MAC `44:1B:F6:C1:34:10`，
  只写应用0x10000；stopwatch不打开，照片/设置分区不写入。回退首页29e7cb90…保留。
- 本轮未改正式算法、未再次烧录、未提交/推送。颜色阶段完成后再做人像局部优化。

## 上轮进度：青色平滑A/B已烧录，待同屏实拍验收（2026-09-11）

- 用户“已进入”后，新鲜枚举匹配PaperColor下载USB序列号，并在同一打开连接读取芯片MAC，
  均为 `44:1B:F6:C1:34:10`；只操作Color，stopwatch未打开。
- 已写 `.cache/firmware/cyan-ratio-ab-v1/paper_color.bin`，3,263,888字节，
  SHA-256 `938fc5700ef1fe9c64042ec25a9626e510f86171bb3ccd79d41b21cb1346c77d`。
  仅应用0x10000，写入26.9秒，Hash of data verified，退出码0；manifest和flash.log已保存。
  照片/设置分区未写入。已请求RTS重启，需松开Boot，必要时单按Reset。
- 烧录前重新核对候选二进制、配置和87项匹配源码/素材快照哈希；本轮未改算法、未重建。
- 下一张应为 `CYAN RATIO SMOOTH A/B`，四列A/B/B/A。先查重复列与16/17/10保护，
  再看20青色及两条渐变的偏色、亮度、条带和颗粒；此图不是完整24色回归。
  B候选尚未物理通过，不宣称校色完成。A键返回首页会使用B照片策略，首页修复保留。
- 回退首页29e7cb90…及旧配比对照757f9353…均保留；不提交/推送校色实验。

## 上轮进度：青色平滑A/B候选已构建，待Color连接并进入下载模式

- 按用户“好的 继续”，将IMG_9955青色少绿方向实现为默认关闭的CYAN_RATIO_SMOOTH。
  仅绿蓝域，色相/彩度/颜料份额平滑衰减，最多转移彩色部分6.25个百分点绿到蓝，
  Q4前白黑守恒。洋红保持原配比，未采用两点硬匹配探针。
- 65,536目标中5,081改变；191,488邻接边未新增超过30的目标边，全域最大59.7874保持。
  既有青蓝压缩/域边界不连续未修复。14均匀色、20保护渐变逐字节一致；
  共28条渐变中受影响8条仍有局部抖动列变化，待实拍，不称纹理已改善。
- 完整表22个色块裁剪一致；20的B裁剪逐字节等于IMG_9955青色B，白951→953，
  绿1554→1354、蓝1623→1821、黑0。24因扩散残差54像素相位变化、黑码404→402，
  无新增彩色；其余块外12像素变化。A完整帧effced8d…，B为c675e6c1…。
- 基线/候选及几何、暖色、紫色ASan/UBSan通过；全候选开关的真实首页适配器测试通过。
  原生码采样整帧/非法载荷/越界与独立组图一致；实际CMake哈希检查拒绝篡改素材。
- 新图CYAN RATIO SMOOTH A/B，A/B/B/A：20青色、两条色相/低彩度渐变、16/17/10保护。
  素材26,400字节，8d96879b…；详细方案 `tools/color_lab/CYAN_RATIO_SMOOTH_CANDIDATE.md`。
  回首页会使用候选B颜色策略，首页区域渲染修复保持3c255db，尚未进行候选物理验收。
- ESP-IDF构建成功，`.cache/firmware/cyan-ratio-ab-v1/paper_color.bin`，3,263,888字节，
  SHA-256 `938fc5700ef1fe9c64042ec25a9626e510f86171bb3ccd79d41b21cb1346c77d`，
  checksum f7、validation hash有效；配置/87项源码与素材快照、测试/构建记录已保存。
- **尚未烧录。** 最新枚举只见系统串口，未见Color；本轮未打开任何串口。
  待Color接回进入下载模式，重新核对USB和同连接MAC `44:1B:F6:C1:34:10`，
  仅写应用0x10000；stopwatch不打开，照片/设置不写入。勿照搬构建输出的全盘flash命令。
- 最后验证烧录仍为IMG_9955配比对照757f9353…；回退首页29e7cb90…保留。
  实拍先查重复列和16/17/10，再查20及两条渐变；通过后再完整24色物理回归。
  本轮未提交/推送校色实验，人像阶段仍在后。

## 上轮进度：IMG_9955配比对照已复核；青色B方向待设计，洋红保留A

- 本张为预期的secondary-ratio-abc-v1，完整逐项见 `tools/color_lab/IMG_9955_REVIEW.md`。
  两端A总体接近；17浅紫保持，10/19规则左缘点列未复发；22灰网纹仍明显。
- 20青色B少绿后偏绿减轻、更趋蓝青，C更偏黄绿。只采纳下一轮探索方向，
  尚未认定B准确或6.25个百分点为最优；白点、灰暗感未解决，不继续加大减白。
- 16洋红B偏蓝灰、C偏红褐，无明确优于A的证据，正式算法暂保留A。
  控制行是固定A复制，不能当成尚未实现的广域候选已经通过回归。
- 本次核对缓存应用757f9353…及40项固件相关源码/素材/探针哈希一致。
  USB新鲜匹配PaperColor运行口303A:4002、产品与位置8-3.3，图表success=1，
  total17.624527 s、render0.011951 s、panel17.612571 s；原始记录
  `artifacts/color_calibration/IMG_9955-secondary-ratio-abc-usb.txt`。stopwatch未打开。
- 下一步独立设计青色少绿、平滑衰减的候选，暂不改16；保持白/黑及原生绿/蓝、10保护，
  先检查全RGB565连续性、渐变与完整24色，再准备实拍。不把主机两点探针用于正式照片。
- 首页修复已按用户要求独立commit并push：`3c255db`，远程feat/papercolor-os已核对；
  独立主机测试与固件构建通过。校色实验仍为本地未提交改动。
- 当前设备仍为配比对照757f9353…；本轮只复核与记录，未改颜色系数、未构建/烧录新候选。
  无需重复同图实拍。颜色阶段继续，人像局部优化后做。

## 上轮进度：首页人工验收通过，恢复校色；16/20配比对照已烧录

- 用户明确“我已人工校验，通过；继续校色任务吧”。首页缩略图/Logo修复标为通过，
  不要求重复照片；已在home-photo-pipeline候选manifest及修复文档记录用户验收。
- 本轮恢复16/20：已有减白实拍存在变沉代价，不继续加大减白。
  新图A/B/C/A固定白/黑目标，B/C分别在彩色部分减少/增加6.25个百分点红或绿。
  16的红占比约45.61/39.36/51.86%，20的绿占比48.91/42.65/55.21%；
  实际白码最多差4个，黑码均0。17/10/19/22固定使用A裁剪，四列逐字节一致。
- 实验详见 `tools/color_lab/SECONDARY_RATIO_ABC_CHART.md`。B/C仅是主机临时两输入目标
  扰动，不能直接用于一般照片；固件正式颜色算法未改，A键回首页仍保留已验收的UiWithPhotos。
  探针在ESP_PLATFORM下禁止编译，避免把两点硬切换误当成正式校色策略。
- 65,536目标扫描每版只改变16/20对应的两个RGB565输入；Q4前白/黑守恒，Q4后按
  独立舍入传播上界检查。A完整帧effced8d…保持IMG_9952；采样器整帧/非法码/越界、
  探针守恒均通过ASan/UBSan；实际CMake正确/篡改载荷检查通过。
- 新素材39,600字节，载荷749aa3cd…，图表不是完整24色回归。先查两端A及固定控制，
  再分别比较16/20三种配比的色相、明暗和颗粒；不预设更多红/绿自动更准确。
- 应用已构建并烧录：`.cache/firmware/secondary-ratio-abc-v1/`，3,276,336字节，
  SHA-256 `757f9353268befb5e018453b92ccb0b11cc3d0a11759f4515bd80da839da5537`，
  checksum ac、validation hash有效。相对人工验收首页版仅开启新诊断启动配置，
  首页模块/区域适配器/照片算法哈希核对未变。
- Color已处下载，重新核对USB及同连接MAC `44:1B:F6:C1:34:10` 后仅写应用0x10000，
  27.1秒，哈希验证成功、退出码0。stopwatch未打开，照片/设置分区未写入，RTS重启已请求。
- 当前等待松开Boot、Reset后整张配比对照实拍；尚未选B或C，也未修改正式照片系数。
  回退为已人工通过的首页镜像29e7cb90…；后续若方向明确再设计平滑广域候选并全表回归。
  未提交/推送，颜色阶段完成后再做人像。

## 上轮进度：按用户要求暂停色表调参；首页颜色策略修复已烧录

- 用户发现首页缩略图、Logo与图片浏览器显色不同，优先修复此问题。16/20等调参暂停。
- 已确认根因：首页完整/局部提交用默认Ui最近色映射，浏览器用PhotoBalanced；
  本地/Ezdata缩略图及Logo只解码到Canvas，随首页走Ui，没有使用浏览器的照片策略。
- 修复：新增UiWithPhotos；缩略图和Logo独立PhotoBalanced，UI文字/线框仍按Ui，
  共用一次刷新，不二次量化、不改颜色系数。两区域扩散隔离，工作区4,776字节。
  详见 `tools/color_lab/HOME_PHOTO_PIPELINE_FIX.md`。
- 真实显示适配器+照片算法主机ASan/UBSan通过：同尺寸浏览器输出精确一致、外部UI不变、
  四旋转/RGB565与RGB888/局部裁剪/状态恢复/空图及内存失败回退。管线关闭兼容也通过。
- 正常首页固件构建成功（关闭色表启动、保留IMG_9952算法开关），镜像3,234,032字节，
  SHA-256 `29e7cb9045f24eb1fe4a76974baf54c4251bd91b560c532db8be458284af2604`，
  缓存 `.cache/firmware/home-photo-pipeline-candidate/`，checksum9d/validation hash有效。
- Color已处下载模式；再次核对USB及同连接MAC `44:1B:F6:C1:34:10`，
  仅向应用0x10000写入，26.8秒、哈希校验成功、退出码0。stopwatch未打开，照片/设置未写入。
- 当前应用为上述首页修复，待松开Boot、Reset后首页与同一图片浏览器的物理复核，
  以及新鲜身份核对后的运行指标（预计ui-with-photos）。缩放/裁剪仍会带来网点差异。
  收到照片前不宣称实拍验收通过；不继续色表调参、不提交/推送。

## 上轮进度：IMG_9952完整表回归通过，保留紫色平滑实验基线

- 全24项见 `tools/color_lab/IMG_9952_REVIEW.md`。09/17/18灰紫/深紫保持，
  10/19规则左缘点列未复发；暖色、绿系、灰阶未见明显新增退步。
  结合IMG_9951同屏渐变改善，平滑改动保留；不代表整体颜色还原合格。
- 当前应用仍为457106f2…完整表（`.cache/firmware/purple-smooth-chart-candidate/`），
  缓存二进制、匹配源码/探针/素材哈希再次核对，未读回整幅Flash。
  已验证完整源帧effced8d…与IMG_9949一致，本张只能确认保护保持，不能宣称全表改善。
- USB `reference_chart success=1`，prepare1.078205 s、total17.549576 s、
  render0.419182 s、panel17.130385 s，workspace14,496字节；仅访问身份核对的Color运行口。
  原始记录 `artifacts/color_calibration/IMG_9952-purple-smooth-chart-usb.txt`。stopwatch未打开。
- 新鲜编译匹配私有函数查询24色目标：`artifacts/color_calibration/IMG_9952-mapping-audit.json`。
  03/04/06仍是RGB565量化偏离原生顶点，既有白9/黑30/黑16码不是新边缘缺陷；独立小修待办。
  16/20不触发紫色/青蓝旧源补偿条件，继续调紫色启用阈值不会解决它们的偏色。
- 下一轮优先独立检查16/20目标混色比例及白量，保留09/17/18、10/19保护；
  03/04/06量化、11/12、暖色和灰阶网纹分别排队，不直接再加大减白或按手机RGB拟合。
  当前还没有下一候选；无需重烧同版或重复要求相同色表复拍。颜色优先，人像后做。
- 本轮未改算法、未构建新候选、未再次烧录、未提交/推送。完整旁路仍否决，历史镜像保留。

## 上轮进度：紫色平滑完整24色表已烧录，待实拍及运行回归

- 用户切换下载模式后，重新核对PaperColor USB序列号与同连接芯片MAC
  `44:1B:F6:C1:34:10`，仅打开 `/dev/cu.usbmodem83301`，stopwatch未操作。
- 已写应用 `457106f2e2dd3b39498cc4e0047c09a9ffb3ec1e07f09496bf52e1d683997c93`，
  3,255,696字节，仅地址0x10000，写入26.9秒，哈希校验成功、退出码0。
  缓存 `.cache/firmware/purple-smooth-chart-candidate/` 的manifest/flash.log已更新。
  照片与设置分区未写入；RTS重启已请求，需松开Boot并单按Reset。
- 下一张应为完整24色表；先复核09/17/18、10/19，再逐项检查其他色块与灰阶。
  算法与IMG_9951的B一致，主机完整源帧与IMG_9949逐像素相同，预期原有颜色保持。
  实拍到达后读取已核对身份的Color运行指标，确认动态照片路径正常。
- 仍未完成整体物理颜色验收；宽过渡带、颗粒及其他偏色保持待办。
  本轮只烧录已缓存应用，未改算法、未提交/推送。

## 上轮进度：IMG_9951同屏复核通过，完整色表候选已构建待烧录

- 逐项实拍复核 `tools/color_lab/IMG_9951_REVIEW.md`：中间两列B的蓝紫硬分界缓和，
  第二条渐变改善更明确；09/17/18未见灰蓝/蓝黑退步，10左缘规则点列未复发。
  原生红上部细浅曲线只记局部实拍现象，不归因渐变算法或单张断定划痕。
- 平滑启用暂保留；第一条仍有宽蓝色过渡带、黑白颗粒变化，紫色灰感/纹理未解决。
  不称完全自然或整体颜色校准通过。本图只有4个源图控制与6原生锚点，不冒充全24项。
- USB `purple_smooth_ab_chart success=1`，total17.592918 s、render0.012348 s、
  panel17.580566 s；原生码诊断耗时不能当动态照片性能。仅打开身份核对后的Color运行口，
  stopwatch未打开。原始记录 `artifacts/color_calibration/IMG_9951-purple-smooth-ab-usb.txt`。
- 沿用完全相同算法，不叠加参数。完整源色表候选已构建：
  `.cache/firmware/purple-smooth-chart-candidate/paper_color.bin`，3,255,696字节，
  SHA-256 `457106f2e2dd3b39498cc4e0047c09a9ffb3ec1e07f09496bf52e1d683997c93`，
  checksum dd、validation hash有效。缓存包含配置、匹配源码、构建/图像验证记录。
  对比本次A/B只切换启动页面；相对IMG_9949仅开启PURPLE_COMPENSATION_SMOOTH。
- 既有主机完整源帧与IMG_9949逐像素相同（effced8d…），且全部算法/探针/测试源码
  哈希再次核对未变；无需重新跑相同大批测试。新ESP-IDF构建及镜像验证通过。
  下一张用于完整24项与动态照片运行路径回归，预期色块保持，不宣称有额外校色改善。
- 当前设备仍是IMG_9951同屏图的ed7dfdbe…应用；完整表457106f2…尚未烧录。
  等用户将Color切入下载，重新核对USB和同连接MAC `44:1B:F6:C1:34:10`，
  仅写应用0x10000。stopwatch不操作、照片/设置不写入。
- 本轮未改算法、未提交/推送。完整回归后继续16/20、11/12、暖色、近原生和灰阶纹理，
  先完成颜色阶段再进入人像局部优化。

## 上轮进度：紫色平滑同屏 A/B 已烧录，待实拍验收

- 用户已切换下载模式，重新核对PaperColor USB序列号及同连接芯片MAC
  `44:1B:F6:C1:34:10`，仅操作 `/dev/cu.usbmodem83301`；stopwatch未打开。
- 应用SHA-256 `ed7dfdbe170c73eebd71e1bdb379e0534e37178ed8990ebf046f4a558489bc3e`，
  3,261,680字节，仅写0x10000，用时27.0秒，`Hash of data verified`，退出码0。
  缓存manifest及flash.log已记录；照片/设置分区未写入。RTS请求重启已发出。
- 下一步松开Boot、单按Reset；等屏幕完整刷新后拍整张 `PURPLE TRANSITION A/B`。
  先复核09/17/18、10四行控制，再比较前两行渐变的突变/颗粒/黑白量。
  A旧硬阈值、B平滑，列A/B/B/A；新方案尚未物理验收。
- 回退镜像仍为 `.cache/firmware/warm-bypass-chart-candidate/paper_color.bin`
  （6f02d38c…）；本轮未再改算法、未提交/推送。

## 上轮进度：紫色阈值平滑 A/B 已构建，待Color下载模式（尚未烧录）

- 详情 `tools/color_lab/PURPLE_SMOOTH_AB_CHART.md`。基于IMG_9949已恢复的暖色旁路版，
  平滑旧蓝紫补偿启用区间R/B=2/5..5/8，保留区间上方原增红强度。
  新开关默认关闭；完全紫色旁路仍否决。窄区间草稿新增324条目标大跳变，已否决留档。
- 65,536目标/191,488邻接边：同域旧阈值最大68.30→28.00，全域68.30→59.79，
  无新增超过30的邻接目标边；候选域边界及青蓝旧分支仍有跳变，未称全部连续。
- 全24色240,000像素与IMG_9949逐字节一致，16均匀色/20条保护渐变一致。
  原09/17/18、10/19、暖色和灰阶保护项未变，不把保持原样说成进一步校色改善。
- 新图A/B/B/A：两行专门跨阈值的渐变，加09/17/18/10四行原图控制和六原生锚点。
  两种完整源帧经实际照片流水线后裁剪，原生码直送，不二次抖动；不是原色表自带渐变。
  单列抖动诊断仍有局部退步，必须实拍比较颗粒/条纹/黑白量后再决定是否保留。
- 旧版/候选完整主机测试和ASan/UBSan通过，新阈值测试旧版负对照能检出原跳变。
  过渡区RGB888(90,0,165)现含约4.7%黑，显式检查新比例；真正双色边界仍严格保护。
  新采样器整帧/非法载荷/越界检查通过，实际CMake哈希检查能拒绝篡改载荷。
- ESP-IDF构建成功，应用3,261,680字节，SHA-256
  `ed7dfdbe170c73eebd71e1bdb379e0534e37178ed8990ebf046f4a558489bc3e`，
  checksum62/validation hash有效。缓存 `.cache/firmware/purple-smooth-ab-v1/`
  含镜像、配置、源码/素材快照、构建与验证记录；尚未烧录、尚未物理验收。
- 最新USB枚举：PaperColor 303A:4002、8-3.3处于运行模式；下载模式8-3.4为stopwatch，
  未打开任何串口。待用户切Color下载后重新核对USB序列号及同连接MAC
  `44:1B:F6:C1:34:10`，仅写应用0x10000；当前设备仍为IMG_9949对应的6f02d38c镜像。
- 不更改存储/设置/驱动波形，不提交或推送。实测先复核保护行，再看两条渐变，
  若可接受再完整24项回归，最后才进入人像局部优化。

## 上轮进度：IMG_9949回退实拍通过，后续以IMG_9944暖色版为起点

- 完整24项见 `tools/color_lab/IMG_9949_REVIEW.md`。09/17/18灰紫和深紫表现恢复，
  IMG_9947明显灰蓝/蓝黑退步已撤回；10/19规则边缘点列未复发，暖色及灰阶保持。
  本次仅确认回退，整体颜色仍未合格，16/20及其他偏色/颗粒继续待办。
- 05中下部不规则浅痕本张不明显，01/19仍有细浅痕；单一原生码区域的局部实拍现象
  不归因新抖动，也不据此判固定坏点。没有调整照片颜色或驱动。
- USB `reference_chart success=1`，prepare 1.098393 s、total 17.623907 s、
  panel 17.205997 s、workspace 14,496字节；仅打开PaperColor身份核对后的运行口。
- 当前应用 `6f02d38caac15e324751d9691e197ea7248f7387ac0539316c79067154ab357d`，
  缓存镜像、配置及匹配历史源码/未改变探针哈希再次核对；不是把当前工作区实验源码
  假称为旧镜像源码。stopwatch未操作；无需再烧录同版或重复要求相同色表复拍。
- 下一步先离线研究平滑旧蓝紫增红启用边界，约束09/17/18红蓝与黑白量，再查全域
  连续性、渐变和完整表保护项；新方案尚未实现。完全旁路继续否决，旧证据全部保留。
- 本轮未改算法、未构建新候选、未再次烧录、未提交/推送。

## 上轮进度：蓝紫旁路已否决，已回退IMG_9944暖色完整色表

- 全24项见 `tools/color_lab/IMG_9947_REVIEW.md`。09/18更偏蓝黑、17白点减少但
  转灰蓝，紫色特征减弱；不采用完全旁路，不把目标连续性当作物理还原通过。
- 根因与已知色码变化一致：09/18红码大幅减少并增黑，17减少白、增加蓝。
  10/19规则边缘点列未复发，暖色及中性未见明显新退步；其他偏色仍未解决。
  05中下部、08/19等有不规则细浅痕，软件填充区单一原生码，不归因新增抖动白点。
- USB `reference_chart success=1`，prepare 1.107795 s、total 17.796859 s、
  panel 17.378411 s、workspace 14,496字节；应用和全部源码哈希匹配，stopwatch未访问。
- **回退已完成。** 已核对IMG_9944暖色旁路完整色表缓存，应用3,255,552字节，
  SHA-256 `6f02d38caac15e324751d9691e197ea7248f7387ac0539316c79067154ab357d`，
  `.cache/firmware/warm-bypass-chart-candidate/`，checksum/validation hash有效。
  恢复旧蓝紫增红，保留暖色旁路/减白/配比/边缘修复；该基线也不是最终校色合格版。
- 用户切换后，重新核对USB及同连接MAC `44:1B:F6:C1:34:10`，仅向PaperColor
  `/dev/cu.usbmodem83301` 应用0x10000写入3,255,552字节，26.9 s，
  `Hash of data verified`，退出码0；已请求RTS重启，需松开Boot、单按Reset。
  stopwatch端口未打开，照片/配置分区未写入。当前应用已恢复上述IMG_9944版本。
- 下一步研究平滑旧增红启用边界，并约束已保留色块的红蓝/黑白量；
  新方案尚未实现。颜色优先、继续全表回归，不直接去除增红或按手机RGB拟合系数。
- 本轮仅回退保存的应用镜像，未改源码、未提交/推送；历史镜像与测试证据全部保留。

## 上轮进度：蓝紫旧补偿旁路已烧录，待完整色表实拍

- 用户要求继续，先隔离审查蓝紫/青蓝旧补偿。详见 `tools/color_lab/PURPLE_BYPASS_CANDIDATE.md`。
  青蓝直接旁路会令10出现黑10/白216，暂不采用；只做默认关闭的蓝紫旁路候选。
- `PAPERCOLOR_PURPLE_COMPENSATION_BYPASS` 仅跳过旧红蓝增红减蓝，暖色旁路、
  青蓝压暗、配比/减白/投影/扩散与边缘保护均固定。完整色表仅09/17/18目标改变。
- 全RGB565仅原域6,827目标改变，相关最大目标跳变68.30→20.92，全局仍59.79，
  没有新增跨30边。14均匀控制场和19/24渐变逐像素不变；5蓝紫渐变主动改变，
  原始单列振荡变大项完整保留，尚不判定实物渐变/颜色通过。
- 09黑140→924、18黑1184→1744、17白634→306；可能过暗/偏蓝。10仍黑白0，
  19全蓝；其余单项最大计数变化6像素。旧增红比例和旧紫色边界目标测试不再适用
  的原因已记录：基线约束保留，候选检查真实新混色，另加两版都真实处于红蓝线的保护源。
- 默认/实验基线host、候选ASan/UBSan、两滤波器/五邻色/灰阶/纯色、阈值新旧负例、
  H5、10项Python及独立ESP构建通过。软件数值通过不等于物理还原通过。
- 应用3,255,504字节，SHA-256 `e35ef03346dbd776195875dd153871869d71897ca0daa7cfd0a53a740c4e8514`，
  `.cache/firmware/purple-bypass-chart-candidate/`，镜像校验有效。相对IMG_9944仅新增
  蓝紫旁路开关，回完整24色表。旧AB素材不覆盖，源码哈希失配时回退用保存镜像。
- **已烧录，尚待实拍。** 用户确认进入下载模式后，核对USB序列号及同连接
  芯片MAC `44:1B:F6:C1:34:10`，仅向PaperColor `/dev/cu.usbmodem83301`
  应用0x10000写入3,255,504字节，27.0 s，`Hash of data verified`，退出码0。
  已请求RTS重启；需松开Boot、单按Reset后拍摄完整24色表。stopwatch端口未打开。
  当前设备应用为蓝紫旁路候选；未提交/推送，照片/配置分区未写入。
- 下一轮先09/17/18过暗/偏蓝/颗粒，再10/19、暖色13/14/15及全24项；风险明显则
  回退或另做连续映射，不以阈值修复为由强行保留颜色更差的方案。

## 上轮进度：IMG_9946暖色同屏已判读，暂留B旁路作实验起点

- 完整六行记录 `tools/color_lab/IMG_9946_REVIEW.md`。14的B略浅、亮暗纹理反差
  稍弱，但两版仍偏金黄/黄褐，亮橙还原未通过。暂留B与已验证阈值连续性修复，
  不继续加白，不提升整套参数为物理校色基准；没有明显控制项退步要求立即回退。
- 同字母整体色调相近，但同码控制格也有细纹方向/锐度差异，不把所有网纹变化
  归于算法。13/15未见明显新退步，10/19边缘保护保持，22中性保护保持。
  顶部全黑参照细浅痕继续记录，不据此改驱动。未显示的其他源色沿用IMG_9944待办。
- USB `warm_ab_chart success=1`，total 17.679145 s、render 0.011528 s、
  panel 17.667614 s，无照片prepare；仅打开已核对PaperColor运行口，stopwatch未访问。
- 下一步回完整色表流程，先分别离线审查蓝紫/青蓝两段旧补偿与当前投影/配比
  的叠加、全RGB565目标连续性和渐变，再选单变量候选。优先09/17/18与10；
  16/20原先不触发这些分支，不把所有偏色归因旧补偿。暖色/纯色/边缘继续保护。
- 本轮应用、素材和全部源码哈希核对通过，未改算法、未新建候选或再次烧录，
  无提交/推送。设备仍为暖色A/B诊断 `976a2dc42a960063ba5842c6535a185d254569e6daa80e3fe666f0cd4eea7ea4`。

## 上轮进度：暖色同屏A/B已烧录，待实拍

- 用户要求继续，已完成 `tools/color_lab/WARM_AB_CHART.md` 所述诊断。A旧补偿
  完整源帧复现IMG_9938，B旁路复现IMG_9944；只切该分支，其他减白/配比/保护固定。
- 每行A/B/B/A，行14/13/15/10/19/22；真实照片整图渲染后裁块，设备原生输出，
  不重新抖动。重复格完全相同；13/10/19/22的A与B也逐像素相同，15仅点阵位置不同。
- 14白241→467（5.838%→11.313%）、黄1484→1385、红2403→2276；仍需同屏
  判断提亮、变灰与织纹代价，未把任何一版提升为物理校色基准。
- 26,400字节素材经独立全帧拼接与实际C++采样器核对；ASan/UBSan、非法素材
  拒绝、诊断关闭编译、CMake哈希正负检查、默认host/H5/10项Python及ESP构建通过。
- 应用3,261,520字节，SHA-256 `976a2dc42a960063ba5842c6535a185d254569e6daa80e3fe666f0cd4eea7ea4`，
  `.cache/firmware/warm-ab-chart-v1/`，checksum/validation hash有效。相对IMG_9944
  仅切诊断启动，旧减白ABBA和历史镜像均未覆盖。
- **已烧录，尚待实拍。** 用户确认进入后，核对USB序列号及同连接芯片MAC
  `44:1B:F6:C1:34:10`，仅向PaperColor `/dev/cu.usbmodem83301` 应用0x10000
  写入3,261,520字节，26.9 s，`Hash of data verified`，退出码0，已请求RTS重启。
  stopwatch端口未打开；当前设备应用为暖色同屏A/B诊断。
- 需松开Boot、单按Reset，等刷新结束后拍摄整个A/B/B/A图。未改照片算法、
  未写照片/配置分区、未提交/推送；先14，再13/15与边缘/灰阶控制。

## 上轮进度：IMG_9944全24项复核完成，暖色收益尚未通过验收

- 详见 `tools/color_lab/IMG_9944_REVIEW.md`。14仍呈黄褐/金黄的高反差织纹，
  与原图亮橙差距明显；软件阈值连续性已修复，但新增白份额的实物收益未确认。
  当前应用仅保留作实验对照，不提升为校色基准，不继续增加白色或叠加红黄系数。
- 10/19左缘点列未复发；16/17仍灰紫、20仍偏灰绿，09/18、11/12及灰阶纹理待办。
  13/15未见明显新退步；03/04/06近原生量化仍在。01细浅痕、05/07孤立亮点
  与全填充区原生码不符，记录实拍现象，尚不归因软件或修改驱动。
- USB `reference_chart success=1`，prepare 1.097404 s、total 17.684744 s、
  panel 17.266705 s、workspace 14,496字节；仅打开身份核对后的PaperColor运行口。
  原始记录 `artifacts/color_calibration/IMG_9944-warm-bypass-usb.txt`，stopwatch未访问。
- 下一步优先同屏暖色A/B：A旧暖色分支、B旁路，固定其他参数，以完整原图实际
  渲染后裁块比较14，保留13/15和边缘/灰阶控制。该诊断尚未制作。
- 本轮已核对保存应用和源码哈希，未改运行算法、未再次烧录、未提交或推送。
  当前设备仍为暖色候选 `6f02d38caac15e324751d9691e197ea7248f7387ac0539316c79067154ab357d`。

## 上轮进度：暖色旧补偿旁路候选已烧录，待完整色表实拍

- 用户要求继续；已定位14源RGB565 `(255,101,49)` 被旧规则先改为
  `(223,101,36)`，规则有B>=32和R-G>=80硬阈值。默认关闭的
  `PAPERCOLOR_WARM_COMPENSATION_BYPASS` 只旁路此分支，不新增经验系数。
  详见 `tools/color_lab/WARM_BYPASS_CANDIDATE.md`。
- 65,536输入仅原暖色域1,638目标改变；相关边最大目标差49.84→28.86，
  没有新增跨30诊断阈值的边；全局最大68.30不变，其他旧补偿仍在。
- 全表14白5.838%→11.313%、红58.212%→55.136%、黄35.950%→33.551%。
  可能提亮也可能变灰/颗粒加重，尚不判定物理还原改善。13/15目标及计数保持。
  当前减白幅度、彩色比和边缘保护固定；10黑白0，19全蓝。
- 15个均匀控制场、16条非暖色渐变逐字节不变；6条暖色渐变含纹理变化
  均保留记录。全图14,652码变化，除14外单项最大计数变化3像素。
- 原橙色白<10%测试继续约束开关关闭的基线；实验按独立投影预期检查更高白量，
  不冒充原视觉验收通过。默认host、候选ASan/UBSan、两滤波器覆盖、阈值测试、
  H5、10项Python及ESP构建通过；旧代码运行新连续性测试失败。
- 应用3,255,552字节，SHA-256 `6f02d38caac15e324751d9691e197ea7248f7387ac0539316c79067154ab357d`，
  `.cache/firmware/warm-bypass-chart-candidate/`；checksum/validation hash有效。
  相对IMG_9938完整色表仅新增暖色旁路开关；恢复完整色表、关闭ABBA启动。
- **已烧录，尚待实拍。** 用户确认Color进入下载模式后，重新核对USB序列号
  及同连接芯片MAC `44:1B:F6:C1:34:10`，仅向PaperColor
  `/dev/cu.usbmodem83301` 应用地址0x10000写入3,255,552字节，26.9 s，
  `Hash of data verified`，退出码0；已请求RTS重启。
  stopwatch也在下载模式，序列号 `44:1B:F6:C1:8A:00`，其端口未打开。
  当前设备应用为暖色旁路完整色表候选；需松开Boot、单按Reset后拍摄完整色表。
  先检查14提亮/变灰/颗粒，再13/15及全24项；所有历史镜像保留，无提交/推送。

## 上轮进度：IMG_9942 同屏A/B已判读，暂留当前减白幅度

- 完整六行对照见 `tools/color_lab/IMG_9942_REVIEW.md`。同字母重复格大体一致，
  16/20的B白雾感较轻、颜色更浓但也更沉；17差异很小。当前幅度暂留作实验基准，
  不继续加大、不宣称整体颜色已准确。16仍灰紫、20仍偏绿，不能靠减白独立解决色相。
- 10两版边缘无点列，19全蓝、22中性保护保持。顶部黑参照左下有极细浅痕，
  素材为全黑，先记录实拍现象，未据此归因算法或修改驱动。
- 此图是六行24格，不是完整24种色块；缺席的暖色等沿用IMG_9938待办。
  下一轮回完整源色表，优先分析14旧暖色补偿和连续性，联合13/15，保持白份额
  及彩色比固定、全表回归。16/17/20等剩余偏色和纹理继续保留，不标记校色完成。
- USB `white_ab_chart success=1`，总17.670932 s、render 0.012233 s、refresh
  17.658696 s；原生预渲染图没有设备照片prepare，不与普通照片处理耗时混比。
  应用、素材及源码哈希匹配；只查询身份已核对的PaperColor运行端口，其他设备未打开。
- 当前设备仍为ABBA诊断应用 `ff5e39a31dc9fc1fa119584b4401b7fb8cd3c0edad5368a04de81c900dbe5f76`。
  本轮未改运行算法、未再次烧录，未提交/推送；所有历史镜像保留。

## 上轮进度：2026-09-10 同屏照片算法减白 A/B 已烧录

- 用户要求继续，已落实同屏对照。详见 `tools/color_lab/WHITE_AB_CHART.md`。
  每行A/B/B/A，A关闭温和减白、B开启，两版保持彩色比和已验收边缘保护。
  行为16/17/20/10/19/22，顶部六原生参照；同字母重复格逐字节一致。
- 主机编译实际照片代码，完整RGB565源图先PhotoBalanced渲染、后裁块，
  保留原图扩散上下文；B整图色码与IMG_9938完全一致。设备按原生码显示，
  不缩放或二次抖动。不是IMG_9936那种另造排列的混色图，也不运行时改照片参数。
- 白份额16约18.653%→13.929%，17约17.175%→15.359%，20约30.838%→22.965%；
  10两版黑白码均为0，19全蓝。10/22仍有轻微整图相位差，不声称其A/B像素全同。
- 素材26,400字节，24格与六参照经独立坐标循环和实际C++采样器全图核对；
  CMake会拒绝源码/素材哈希失配。默认host、候选ASan/UBSan、H5、10项Python、
  诊断关闭编译和ESP构建通过；镜像checksum/validation hash有效。
- 诊断仅切换启动入口，照片算法源码和开关保持IMG_9938；新应用3,261,504字节，
  SHA-256 `ff5e39a31dc9fc1fa119584b4401b7fb8cd3c0edad5368a04de81c900dbe5f76`，
  `.cache/firmware/white-ab-chart-v1/`；预览/比例/素材在 `artifacts/color_calibration/white-ab-v1.*`。
- **已烧录，尚待实拍。** 用户准备好设备后，重新核对USB序列号及同连接
  芯片MAC `44:1B:F6:C1:34:10`，仅向PaperColor `/dev/cu.usbmodem83301`
  应用地址0x10000写入3,261,504字节，26.9 s，`Hash of data verified`，退出码0。
  设备应用现为同屏ABBA诊断；已请求RTS重启，需松开Boot、单按Reset后拍摄全图。
  其他设备未打开，照片配置未写入；IMG_9938完整色表镜像保留，未提交/推送。
- 实拍先核对重复格一致，再看前三行B是否减少灰感而不过暗、不增加颗粒。
  此图不等于完整校色通过；确定减白去留后回全24色表，继续暖色及其他混色。

## 上轮进度：IMG_9938 边缘修复实拍通过，全24项复核完成

- 10号左缘规则亮点列已不可见，未见明显新增大条纹；19纯蓝和07红08绿
  保护保持，本轮边缘修复通过。完整记录 `tools/color_lab/IMG_9938_REVIEW.md`。
- 整体颜色仍未合格：16/17灰紫、20灰绿、10较沉，14红黄织纹强，11/12及
  09/18混色仍需优化。03/04/06量化杂点和灰阶网纹没有因边缘修复而关闭。
- 当前应用保持 `8060677d4e877b3821af9548a892d243d94cb6578e833b46f568c2f0a201086b`，
  作为后续实验起点；仅边缘修复验收，不把整套减白/配比参数提升为校色基准。
- 下一步回到颜色映射，优先同屏实际照片渲染的减白开/关对照，固定彩色比和
  边缘保护，确认白份额实验是否值得保留；暂不继续加大减白。暖色旧补偿单独处理。
- 本轮保存应用和色彩源码哈希匹配。最初可见运行端口，准备读取metrics时设备
  已不在枚举中；身份筛选在打开串口前终止，未取得新耗时，其他设备未访问。
- 本轮未改运行算法、未再次烧录，未提交/推送；各历史镜像继续独立保留。

## 上轮进度：10 号边缘修复候选已烧录

- 用户要求继续优化。实现默认关闭 `PAPERCOLOR_CHROMATIC_EDGE_GUARD`，
  仅在红蓝/绿蓝连线目标即将选出异色码时，保留切向误差并改选这两种彩色；
  兼容的既有选码和残差保持。详见 `tools/color_lab/CHROMATIC_EDGE_CANDIDATE.md`。
- 实际完整色表：10 黑1/白10 → 黑0/白0，蓝2696、绿1432；19继续全蓝。
  所有65,536个Q4目标不变，15个均匀控制场及16条渐变中的12条逐字节一致。
  不改变16/17/20真实白色目标、不增减白幅度、不改FS/Burkes权重。
- 4条渐变仍有网点相位变化，9列移动均值最大逐通道差2.8879/255，原始
  单列变化记录完整保留；这不是物理色差或纹理合格证明。全表11,478码改变，
  除10外最大覆盖计数变化9像素，不声称只影响11个异常点。
- 先前对所有连线像素投影的初版未采用、未烧录；最终只拦截不相容选码。
  新边界测试在旧代码上失败，在候选的两滤波器/两彩色对/五邻区上通过。
  默认host、候选ASan/UBSan、H5、10项Python和独立ESP构建通过。
- 固件3,255,552字节，SHA-256 `8060677d4e877b3821af9548a892d243d94cb6578e833b46f568c2f0a201086b`，
  `.cache/firmware/chromatic-edge-chart-candidate/`；镜像checksum/validation hash有效。
  相对IMG_9937配置仅新增边缘保护开关，应用和源码分别存档。
- **已烧录、尚待实拍。** 用户切换下载模式后，重新核对USB序列号及同连接
  芯片MAC `44:1B:F6:C1:34:10`，仅向PaperColor `/dev/cu.usbmodem83301`
  应用地址0x10000写入3,255,552字节，27.0 s，`Hash of data verified`，退出码0。
  设备应用现为边缘修复候选；已请求RTS重启，需松开Boot、单按Reset后复拍完整色表。
  重点检查10左缘点列与新增条纹，随后19、16/17/20及全24项。
  本次枚举未见stopwatch，其他设备未打开；照片配置未写入，未提交/推送。

## 上轮进度：IMG_9937 全表复核完成，10 号边缘风险显现

- 完整24项见 `tools/color_lab/IMG_9937_REVIEW.md`。19号纯蓝无规则白点，
  纯红/绿/蓝及灰阶保护继续通过；16/17/20仍灰，照片尚未证明减白改善还原。
- 10号左缘一列亮点可见，边缘验收不通过。未改候选渲染代码的临时追踪
  复现10白+1黑，完整色码哈希与已烧录候选报告一致；白集中x=109，黑在(110,242)。
  目标Q4未变，近乎位于G–B连线，但入射误差仍能在K/W/G/B集合中选出黑白。
  详见 `artifacts/color_calibration/IMG_9937-edge-residual-trace.txt`。
- 暂不加大减白或再次改彩色配比；下一步单独研究两彩色连线目标的边缘残差
  约束，保留真实混色比例与渐变，不按图块坐标清点、不禁用其他块需要的白。
  该下一步尚未实现；整体候选不通过验收，当前仅保留为实验对照。
- USB确认 `reference_chart success=1`，prepare 1.056691 s，总17.520384 s，
  refresh 17.102906 s，工作区14,496字节；保存镜像及源码哈希匹配。
- 本轮未改运行算法、未再次刷机，其他设备未打开；无提交/推送。
  当前应用仍为减白候选 `99af098d07db736f23d3e3c97332601c157da86d92041469159e7fe2f5f68a48`。

## 上轮进度：温和减白候选已构建并烧录完整色表

用户要求继续推进。详见 `tools/color_lab/SECONDARY_WHITE_CANDIDATE.md`。

- 新增默认关闭 `PAPERCOLOR_SECONDARY_WHITE_REDUCTION`，红蓝/绿蓝混色最多
  减少原白份额25%，按原彩色比例分配；保留原浮点黑份额，近中性/浅色与
  颜料边界平滑衰减。25%是实验步长，不是实测系数或全局白色上限。
- 全表：16白18.60%→13.91%，17白17.18%→15.36%，20白30.79%→23.06%。
  彩色相对份额保持；实物可能变暗，尚未证明还原改善。
- 10目标Q4保持(1417,1433,2824)，但邻区扩散相位变化令左缘白码5→10、
  第二列(110,242)新增1黑码；已明确记录为实拍关注项，不宣称全部边缘零杂色。
  19纯蓝完整边缘仍100%蓝，未额外改变扩散核或扩大保护来掩盖此项。
- 10个独立均匀控制场、4条保护渐变逐字节不变，共11条渐变检查通过。
  65,536目标/191,488边：无新增跨过30诊断阈值的大跳变，最大68.3513→68.2989。
- 默认/候选host、ASan/UBSan、H5、10项Python及独立ESP构建通过，镜像校验有效。
- 新应用 3,254,160 字节，SHA-256 `99af098d07db736f23d3e3c97332601c157da86d92041469159e7fe2f5f68a48`，
  缓存 `.cache/firmware/secondary-white-chart-candidate/`。
- **减白候选已烧录，尚待完整24色表实拍验收。** 用户切换下载模式后，
  重新核对USB序列号及同连接芯片MAC `44:1B:F6:C1:34:10`，仅向PaperColor
  `/dev/cu.usbmodem83301` 应用地址 `0x10000` 写入3,254,160字节，26.8 s，
  `Hash of data verified`，退出码0。照片/配置分区未写入，其他设备未访问。
  本次枚举未见stopwatch；仍按PaperColor精确身份选择，未盲选端口。
  已请求RTS重启；需松开Boot、单按Reset，重点复核16/17/20是否变暗及10边缘。
  IMG_9934应用/色彩源码和IMG_9933边缘基准均保留。

## 上轮进度：IMG_9936 混色图实拍通过，下一轮白份额实验已明确

- 六原生参照及 A/B 两组 40 格、行列标签正常显示。完整观察见
  `tools/color_lab/IMG_9936_REVIEW.md`，诊断图启动与可读性验收通过。
- A 红份额 62.5/75% 已偏红褐，不能继续盲目增红；B 绿份额 62.5/75%
  已偏绿/橄榄，不能把更绿当作青色准确。A37.5–50%、B25–50% 为主要参照。
- 增白会变浅、灰白感增强；12.5/25% 是值得回原色表验证的中间区，
  但不能统一去白。当前图中没有出现鲜艳源洋红/青色的直接对应格，
  仅限本次组合与实拍条件，不宣称整个物理色域绝对不可达。
- 下一轮回到完整 24 色表，固定 IMG_9934 红蓝/绿蓝的相对彩色份额，
  单独试高饱和混色的温和白份额收敛；减少的白按当前彩色比例分配，
  保留黑色目标，平滑保护近中性/浅色及纯色边缘。不能同时再改色相比。
- 10 白已约 0.12%，去白不是它的主要修复方向；重点验证 16/17/20 是否
  减轻灰白且不过暗，09/12/18、暖色、灰阶继续回归。具体候选尚未实现。
- USB `mix_ratio_chart success=1`，总 17.620442 s、refresh 17.606210 s；
  没有照片 prepare 记录，属于原生直通诊断图。
- 设备仍为 `.cache/firmware/mix-ratio-chart-v1/` 诊断应用；本轮未改算法或
  再次刷机，stopwatch 未打开，未提交或推送。

## 上轮进度：同屏 40 格混色比例对照已实现

用户要求继续推进。详见 `tools/color_lab/MIX_RATIO_CHART.md`。

- 新增默认关闭 `PAPERCOLOR_MIX_RATIO_CHART_ON_BOOT`，两组红蓝/绿蓝混色，
  横向彩色份额 25/37.5/50/62.5/75%，纵向白色份额 0/12.5/25/37.5%。
  顶部保留六个原生颜料参照；A 返回正常主页。
- 每格 2,048 像素、40 格精确数量经 C++ 测试及预览生成器独立核对；
  全部使用同一排列模板，直接输出原生码，绕过照片映射/FS。
- IMG_9934 照片算法、LUT 与探针哈希保持不变；本轮只切换启动诊断图，
  不增补偿参数、不写照片库或配置、不改驱动波形。
- 默认 host、候选配置 ASan/UBSan、H5、10 项 Python 和诊断关闭编译通过；
  独立 ESP 构建通过，镜像校验有效。
- 诊断应用 3,232,768 字节，SHA-256 `6a36766b9829f1d769795250768277b36a4f0f7a31afc426fed7d54813777e81`，
  缓存 `.cache/firmware/mix-ratio-chart-v1/`；启用项只切换源色表/混色图启动。
- 标称预览及精确比例：`artifacts/color_calibration/mix-ratio-v1.png/.json`。
  预览不模拟物理屏幕；诊断纹理也不能当作照片 FS 效果验收。
- **40 格混色诊断图固件已烧录，后续 IMG_9936 已确认正常显示，见最新进度。** 重新核对下载模式 USB
  序列号及同连接芯片 MAC `44:1B:F6:C1:34:10`，仅向 PaperColor
  `/dev/cu.usbmodem83301` 应用地址 `0x10000` 写入 3,232,768 字节，26.8 s，
  `Hash of data verified`，退出码 0。stopwatch 未访问，照片/配置分区未写入。
  已请求 RTS 重启；需松开 Boot、单按 Reset 后拍摄完整对照图。
  原完整色表固件和 IMG_9933 边缘修复镜像各自缓存保留。
- 下一张实拍用于在同屏比较彩色份额与白色影响，再决定映射调整；
  不能据此直接宣布绝对 profile、暗部映射或整体校色完成。

## 上轮进度：IMG_9934 实拍复核完成，混色候选仅部分改善

- 完整 24 项见 `tools/color_lab/IMG_9934_REVIEW.md`。
- 16 洋红减轻偏蓝、10/20 增加青绿成分，但仍灰暗/低饱和；17 浅紫改善
  不明显。**尚未通过完整混色还原验收，不提升为完整校色基准。**
- 10/20 规则纹理更显眼。固定偏移相关性检查确认原生码空间分布确有变化，
  不是仅凭手机观感推测；FS 权重未改，比例变化本身也会改变纹理。
- 19 蓝块边缘白点未复发，05/07/08 纯色和灰阶保护继续有效；09/12/18
  未见大幅新退步，但细微差异仍需受控复核。其他色块待办继续保留。
- 设备当前保持比例候选 `031ee00da878fc8c8505a9bf7a2f1c6b6470a9d737704314c6e57de764413d7a`
  作实验对照，未自动回退；IMG_9933 边缘修复应用独立保留。
- 后续应以同屏已知比例混色参照，分辨彩色份额与中性白/物理模型的影响，
  再调整目标；不盲目增加所有紫色的红，不统一去白，不同时改扩散核。
- USB `reference_chart success=1`：准备 1.075607 s，总 17.672744 s，
  refresh 17.254327 s，工作区 14,496 字节。单次测量不作速度提升结论。
- 本轮仅实拍检查、离线纹理归因和 USB 查询；运行算法未改、未再次刷机，
  stopwatch 未打开，未提交或推送。

## 上轮进度：蓝色混色比例候选已完成离线验证

用户已同意按计划继续，优先 16/17、10/20。详见
`tools/color_lab/SECONDARY_BALANCE_CANDIDATE.md`。

- 新增默认关闭 `PAPERCOLOR_BLUE_SECONDARY_BALANCE`，调整红蓝/绿蓝彩色份额，
  保留原浮点目标的黑白系数；低色度、颜料集合边界和单色顶点连续衰减。
  未改调色板、驱动、扩散核或移除旧补偿，不是实测物理 profile。
- 全表模拟：16 红 22.75% → 37.09%；10 绿 26.50% → 34.64%；
  20 绿 25.07% → 33.84%；17 变化较小。09/18 略增蓝，12 也受影响，
  必须完整实拍复核，不预判这些色块已改善。
- 65,536 目标/191,488 相邻边 A/B：没有新增跨过 30 诊断阈值的大跳变，
  最大目标跳变 68.9943 → 68.3513；既有旧补偿硬边界仍在。
- 8 均匀控制场及四条保护渐变逐字节不变；共 11 条渐变已检查。纯红/绿/蓝
  和全蓝块边缘保护保留。完整色表部分其他块发生扩散相位变化。
- 默认 host、候选 host/ASan/UBSan、H5、10 项 Python 回归及 ESP 构建通过。
- 新应用 3,253,328 字节，SHA-256 `031ee00da878fc8c8505a9bf7a2f1c6b6470a9d737704314c6e57de764413d7a`，
  已存 `.cache/firmware/secondary-balance-chart-candidate/`。镜像校验有效，
  启用配置相对 IMG_9933 仅新增蓝色混色比例开关。
- **新比例候选已烧录，后续 IMG_9934 判为部分改善、整体未达标，见最新进度。** 用户进入下载模式后，重新枚举
  USB 序列号并核对同一连接芯片 MAC `44:1B:F6:C1:34:10`，仅向 PaperColor
  `/dev/cu.usbmodem83301` 应用地址 `0x10000` 写入 3,253,328 字节，26.9 s，
  `Hash of data verified`，退出码 0。stopwatch 未访问，照片/配置分区未写入。
  已请求 RTS 重启；需松开 Boot、单按 Reset 后拍摄完整色表。
  IMG_9933 原应用及色彩源码快照保留在 `.cache/firmware/exact-pigment-chart-candidate/`。

## 上轮进度：IMG_9933 边缘修复实拍通过，24 项检查完成

- **19 号蓝块左缘规则白点已不可见，本轮色表修复验收通过。** 与离线
  全填充区 4,128 个蓝码、0 白码一致；不表示物理逐像素无缺陷或整体校色完成。
- 05/07/08 均匀色块保护正常，21–24 灰阶有序且无彩色码污染。
- 完整逐项记录：`tools/color_lab/IMG_9933_REVIEW.md`。第一优先为
  16/17 洋红/浅紫偏蓝灰，以及 10/20 青蓝/青色还原；第二优先为
  09/18 紫色、11/12 黄绿/青绿、13/14/15 暖色混合。
- 24 块最终色码的标称 RGB 平均值与目标的最大逐通道差仅 0.86604，
  支持优先检查目标映射/混色模型，而不是先改扩散核。此为内部一致性指标，
  不是物理色差或 Delta-E。
- 16/20 **未触发旧补偿**；真正触发的是 09/10/14/17/18。不能把所有偏色
  都归咎于旧补偿，也不能全部去白点。03/04/06 近原生量化点列为低优先。
- USB 确认 `reference_chart success=1`：准备 1.084752 s，总 18.020099 s，
  refresh 17.601896 s，工作区 14,496 字节。单次测量不作速度结论。
- 当前设备保持 `.cache/firmware/exact-pigment-chart-candidate/` 应用：
  `1d9a4fa6f38da0b522c51a48f7397886d9c8899720f2c08992aea67dd2e0a37e`。
  本轮未改运行算法或刷机，stopwatch 未打开，先色表后人像方向不变。

## 上轮进度：IMG_9932 完整 24 项复查与蓝块边缘修复

- 用户确认整体有改善，同时指出最左侧小点；明确要求继续逐项检查。
- 已逐项复核 24 个色块，完整填充区含边缘，不再只取中心。
  详见 `tools/color_lab/IMG_9932_REVIEW_AND_FIX.md`。
- 19 号蓝块 12 个白码全部位于左列 x=205，已离线追踪确认：目标精确为
  原生蓝，但传入扩散残差使其被选成白色。不是需要从实拍猜测的硬件故障。
- 新增默认关闭 `PAPERCOLOR_EXACT_PIGMENT_ANCHOR`，只保护精确彩色颜料
  目标；全蓝块变为 4,128 个蓝码，12 个异常白码消除。
- 03/04/06 少量异色码来自源 PNG 经 RGB565 后不再精确等于标称颜料；
  09/10/12/14/16/17/18/20 的物理混色、色相及纹理问题保留逐项待办。
- 11 个均匀控制场、6 条渐变逐字节不变；全色表部分下游网点相位变化，
  最大占比变化 0.218 个百分点。开关关闭的色码哈希与已烧录峰值版本一致。
- 新边界测试覆盖四种颜料、三种邻区、FS/Burkes 双滤波；去掉修复时失败、
  加入修复后通过。默认/候选 host、ASan/UBSan、H5、10 项 Python 及
  独立 ESP 构建通过，镜像校验有效。
- 固件缓存 `.cache/firmware/exact-pigment-chart-candidate/`，3,252,592 字节，
  SHA-256 `1d9a4fa6f38da0b522c51a48f7397886d9c8899720f2c08992aea67dd2e0a37e`。与峰值候选启用项只相差新的边界保护开关。
- **边界候选已烧录，后续 IMG_9933 边缘修复实拍通过，见最新进度。** 用户进入下载模式后重新核对 USB
  序列号及同一连接芯片 MAC `44:1B:F6:C1:34:10`，仅写应用地址 `0x10000`，
  3,252,592 字节，26.9 s，`Hash of data verified`，退出码 0。
  stopwatch 未访问，照片/配置分区未写入；已请求 RTS 重启，需松开 Boot、
  单按 Reset 启动色表并复拍。整体校色尚未完成，先色表、后人像方向不变。

## 上轮进度：IMG_9931 逐项排查与纯蓝峰值修复候选

用户要求逐项检查、先定位明显问题并修复。24 项结论见
`tools/color_lab/IMG_9931_REVIEW_AND_FIX.md`。

- IMG_9931 已确认源色表正常显示。USB：`reference_chart success=1`，
  准备 0.955779 s，总 17.420832 s，refresh 17.002393 s，工作区 14,496 字节。
- 已定位 19 号纯蓝混黑根因：最近 RGB 投影将源 `(0,0,255)` 压到标称蓝
  的约 82.18% 强度；不是驱动码路错误。
- 已实现默认关闭的 `PAPERCOLOR_PRIMARY_PEAK_PRESERVATION` 候选，连续衰减
  跨越色相集合边界；19 号内区由 18.137% 黑 / 81.863% 蓝变为 100% 蓝。
- 7 个均匀控制图及红/中性渐变逐字节保持基准；整张色表有扩散相位变化，
  10 号偏蓝青色及 09/17/18 的邻色影响必须实拍复核。16/20 整体偏色尚未修复。
- host、候选 ASan/UBSan、H5、10 项 Python 回归及独立 ESP 构建通过。
- 候选保存在 `.cache/firmware/primary-peak-chart-candidate/`，应用 SHA-256：
  `85c70316905d8bfc0587d763d70263a2cbe2665c74f5928c4f4e987db4955ca1`。
- **候选已烧录，后续 IMG_9932 确认改善但发现左缘残留白点，见最新进度。**
  用户进入下载模式后，核对 USB 序列号及
  同一连接的芯片 MAC `44:1B:F6:C1:34:10`，仅向 `/dev/cu.usbmodem83301`
  的应用地址 `0x10000` 写入 3,252,480 字节，27.0 s，`Hash of data verified`，
  退出码 0。照片和配置分区未写入，stopwatch 未访问。
- 后续已由 IMG_9932 确认同一色表启动。峰值候选现已被边界修复候选替换，
  见最新进度；峰值候选及源色表基准均保留可回退。
- 线性混色直接替换试验会增加洋红/青色白色比例，本轮未采用；不能宣称已经
  建立物理 profile、完成整体校色或清除了旧补偿。

## 当前方向：先色彩表，后人像（2026-09-09 用户更新）

暂不依赖 IMG_9930 或红帽局部调色。先用既有 24 色块 + 灰阶表尽量
还原整体颜色，再处理人像局部。红帽保留作后续回归，不用于拟合本阶段补偿。
流程见 `tools/color_lab/SOURCE_CHART_CALIBRATION.md`。

- [x] 增加默认关闭的开机源色表入口，直接显示既有 400×600 PNG，
      经 RGB565/PhotoBalanced 处理；A 返回主页，不写照片库或配置。
- [x] 色表完整原生色码 SHA-256 与 21b0370 验收基准一致；保存分块比例
      `artifacts/color_calibration/source-chart-baseline-21b0370.json`。
- [x] 默认关闭入口编译、既有 host/H5 回归通过。
- [x] 独立 ESP 构建通过，3,252,064 字节，镜像校验通过。与已验收配置
      唯一启用项差异为 `CONFIG_PAPERCOLOR_REFERENCE_CHART_ON_BOOT=y`。
      固件及配置已缓存到 `.cache/firmware/source-chart-baseline-21b0370/`。
      应用 SHA-256：`5bb589d5325e1a111832e90b769bbc31d4e68f3803e02d3ae7353528823e5205`。
- [x] 已切换到源色表测试固件：用户进入下载模式后，重新核对 USB 序列号，
      并在同一连接上核对芯片 MAC `44:1B:F6:C1:34:10`；仅写应用地址
      `0x10000`，3,252,064 字节，26.9 s，`Hash of data verified`，退出码 0。
      stopwatch 未访问；照片/配置分区未写入。当前设备应用已不再是红帽测试。
- [x] IMG_9931 与 USB `reference_chart success=1` 已确认源色表启动与基准实拍。
      A 回主页。
- [ ] 按灰阶 → 基本色/橙色 → 紫/洋红/青等混色调整；每轮保留完整色表
      和连续渐变回归。目标比例先于纹理，不同时改补偿、调色板和 FS。
- 物理原生颜料/已知覆盖率混色图作为辅助参照。当前手机实拍只作相对比较。

## 历史设备固件：2026-09-09 红帽测试（已被源色表固件替换）

用户提供 `IMG_9930.HEIC`，已确认红帽测试图显示。与原图对比仍有肤色灰黄、
浅色黄绿色观感及网点掩盖帽纹/发丝的问题；照片白边也偏暖，不能据此定量
校色。原图牙齿/项链本身含色调，近中性候选集合值得单独检查，不能一律去色。
评估见 `tools/color_lab/IMG_9930_REVIEW.md`。本次评估未修改算法或再次烧录。

用户明确选择“继续红帽测试”。已烧录原有测试镜像，颜色算法仍为
21b0370 + 单颜料白色约束，不含本轮新的补偿算法。

- 镜像：`/private/tmp/papercolor-reference-photo-build/paper_color.bin`
- SHA-256：`9bf3b6d063cff88f280a1e11ddd2765d8bb22ce16c3f1f77b8a4a7c132247d16`
- 写入前按 USB 序列号选择 PaperColor `/dev/cu.usbmodem83301`，并在同一
  已打开连接上读取芯片 MAC，均为 `44:1B:F6:C1:34:10`，匹配后才写入。
- 仅写地址 `0x10000`，3,867,008 字节，耗时 32.1 s；esptool 返回
  `Hash of data verified`，退出码 0。未写分区表、storage 或 NVS。
- stopwatch `/dev/cu.usbmodem83401` / `44:1B:F6:C1:8A:00` 未访问。
- RTS 重启后曾仍为下载模式，随后用户操作并提供 IMG_9930，已确认
  红帽图实际启动。尚未验收为自然/准确照片还原。A 键回主页。
- 正常固件的可靠回退镜像仍保存在 `.cache/firmware/accepted-21b0370/`，
  下文“已恢复正常固件”的叙述属于上次暂停历史，不代表现在设备状态。

## 2026-09-09 已接续：离线诊断完成

用户已明确要求从 21b0370 继续。下方“暂停”说明是上次交接历史。
本轮进展详见 `tools/color_lab/TARGET_CONTINUITY_20260909.md`。

- [x] 核实 HEAD=21b0370，保留原工作区人像测试改动。
- [x] 核实已验收回退固件 SHA-256、LUT/单颜料白色约束配置。
- [x] 默认 host/H5 回归、验收配置 host + ASan/UBSan、10 个 Python 测试通过。
- [x] 全 65,536 个 RGB565 颜色、191,488 条格点边、8 条渐变及
      Kodak 04 原图像素分布完成诊断；12 个均匀色块核对实际输出比例。
- 发现旧紫蓝补偿硬阈值：单通道 9 级输入变化引起 83.04 的标称 RGB
  目标跳变；跳过补偿的归因对照为 6.35。并未启用或刷入替代算法。
- 脸颊/肩部采样区触发旧补偿的比例均为 0%，不能把肤色问题归咎于它。
  混色模型与网点纹理仍需分别验证；未添加肤色补偿。
- [x] 用户连接设备后读取 PaperColor USB：`home_full success=1`，总耗时
      17.191134 s，refresh 调用 16.411828 s，UI 准备 0.250606 s。
      原始记录：`artifacts/color_calibration/resume-20260909-usb.txt`。
      这是主页渲染成功记录，不是照片耗时或屏幕外观的独立确认。
- [ ] 实际主页外观及受控实拍仍待确认。
- 后续两项独立实验：紫蓝补偿启用边界的平滑候选；脸颊附近黑白红黄
  已知覆盖率混色的物理比较。运行颜色代码保持基准，无刷机、提交或推送。

### 多设备 USB 身份约束（2026-09-09 更新）

用户明确提示电脑还连着 stopwatch，必须避免错烧。当前枚举为：

- PaperColor 运行模式：`/dev/cu.usbmodem83301`，VID:PID `303A:4002`，
  产品 `PaperColor` / 厂商 `M5Stack`，位置 `8-3.3`；当前运行 USB 不报告序列号。
  已仅向此端口发送诊断查询，返回上述 PaperColor 主页记录。
- 另一台设备（按用户提示视为 stopwatch，禁止操作）：
  `/dev/cu.usbmodem83401`，VID:PID `303A:1001`，序列号
  `44:1B:F6:C1:8A:00`，位置 `8-3.4`。未打开或发送命令。
- **上次的 83401 端口现在属于另一台设备，不可复用历史烧录命令。**
- 每次操作重新枚举；不按第一个串口、VID:PID 或旧端口名盲选。
  如需烧录，先核对 PaperColor 下载模式序列号为
  `44:1B:F6:C1:34:10`，并用 esptool 读取目标芯片 MAC 核实；
  身份不符或不明确则停止，不尝试其他串口。仅写已选定的应用镜像。

## 明天先读这里

用户要求今天停止优化，设备恢复到 **21b0370** 对应的正常固件。
不要自动开始新一轮补偿或刷机；等用户明天明确继续。

- 工程：`/Users/xudanyang/Documents/papercolor`
- 分支：`feat/papercolor-os`
- 本地 HEAD / 已推送提交：`21b0370ae80bac1e23d3484231740e7343999007`
- 保留现有主页、页面交互、上传、照片库、配置和选择记忆，不推倒重做。
- 不要让 Mac 连接设备 AP，曾因此断网无法继续接收指令。使用 USB。
- 本文及该提交之后的红帽人像测试改动仅保存在本地，未 commit / push。
- 工作区保留了默认关闭的测试照片代码，不等于设备仍装着测试固件。
  不要用 `git reset --hard` / `git clean` 删除测试资料。

## 恢复结果与可靠回退点

已直接刷回 21b0370 记录的、IMG_9912 实拍验证过的原始应用二进制，
没有用当前含测试照片改动的工作区重新编译。

- 固件：`/Users/xudanyang/Documents/papercolor/.cache/firmware/accepted-21b0370/paper_color.bin`
- 对应 sdkconfig：同目录 `sdkconfig`
- SHA-256：`a55fbe03ddb32fc8581d7788cfc385283e848e9db3125d9ae3bd73d23265fed7`
- 已向设备 `44:1B:F6:C1:34:10` 的应用地址 `0x10000` 写入 3,228,944 字节，
  esptool 返回 `Hash of data verified`，退出码 0。
- 未写分区表、存储分区或 NVS；照片和配置保留。
- LUT 和单颜料白色比例约束开启；原生校色图启动关闭，不含红帽人像启动功能。
- 本次刷回后的实际主页启动仍待用户松开 Boot、单按 Reset 确认。
- `.cache` 不受 Git 跟踪，但保存在工程目录中，避免只依赖临时目录。

原构建目录 `/private/tmp/papercolor-primary-budget-build` 也保留着同一二进制。
**不要对该目录重新 build / app-flash 来回退**：当前工作区包含后续测试入口，
构建系统可能自动重编译并覆盖已验收二进制。需要回退时先核对 SHA-256，
再直接用 esptool 只写上述保存的应用文件。绝不执行全量 `flash` 或烧写 storage.bin。

如需重建基准，使用 21b0370 的独立干净源码目录，叠加
`tools/color_lab/primary-budget-sdkconfig.defaults`，使用新构建目录。
只 checkout 提交而不启用该配置，不等同于今天实拍的功能配置。

## 已完成，不要重做

1. H5 改为真实上传源图预览，移除与固件不一致的假抖动、白色染灰。
   缩放、旋转、复位、模式选择与两种上传入口保留；预览不冒充物理屏幕效果。
2. RGB565 解码/旋转与原生驱动色码路径已检查；修复误差裁剪导致混色退化，
   使用有符号 Q4 残差、可达色域投影；灰阶限制为黑白，避免彩色污染。
3. 小范围白色比例约束只作用于黑/白/单彩色颜料的三顶点色域。
   纯红采样区白点约 3% -> 0%，IMG_9912 实拍确认规则白点消失。
   紫色、洋红、青色和四级灰色采样区在离线 A/B 中输出像素不变。
4. **全局白色比例约束方案已否决，未刷入**：会让洋红/青色明显偏蓝变暗。
   `white_budget_candidate.cpp` 是研究对照，不能当成最终固件方案。
5. 原生六色色块 + 已知比例混色参考图已用 IMG_9911 验证。
   只能提供定性线索，不能把手机采样 RGB 宣称为绝对校色数据。
6. 既有 host LUT/dither、灰阶、两种滤波器、确定性、H5 源图测试通过；
   单颜料候选另有 ASan/UBSan 与 ESP 编译验证。详见已有校色文档。

## 参考图与证据

- 精确色块原图：`artifacts/color_calibration/papercolor-calibration-v1.png`，
  400×600，配套 JSON 记录 RGB 与采样区域。
- 旧版红色白点：`/Users/xudanyang/Downloads/IMG_9910.HEIC`
- 原生色码及混色参考：`/Users/xudanyang/Downloads/IMG_9911.HEIC`
- 小范围修复后的校色图：`/Users/xudanyang/Downloads/IMG_9912.HEIC`
- 人像原图：`artifacts/color_calibration/kodim04.png`，512×768 sRGB。
  Kodak 04，摄影 Bob Clemens，来源与使用声明见 `tools/color_lab/REFERENCE_PHOTO.md`。
- 人像实拍：`/Users/xudanyang/Downloads/IMG_9914.HEIC`
- 可查看的临时 PNG：`/private/tmp/papercolor-IMG_9914.png` 等；
  临时文件失效时从上述原始 HEIC 重新转换，不修改原始照片。
- 人像测试固件：`/private/tmp/papercolor-reference-photo-build/paper_color.bin`，
  SHA-256 `9bf3b6d063cff88f280a1e11ddd2765d8bb22ce16c3f1f77b8a4a7c132247d16`。
  它使用和 21b0370 相同的颜色算法，只新增独立 Canvas 的开机内嵌人像，
  A 回主页，不写照片库。当前已被正常固件替换。

### IMG_9914 的阶段评价

用户主观评价“中规中矩”。与原图对比：大轮廓、红色方向和五官可辨；
肤色偏灰、局部偏黄；粉紫背景偏灰紫；头发、皮肤与帽子编织细节受到
高对比抖动网点干扰。人像“可看”，但还不能认定为准确或自然的照片还原。
手机光线、曝光、白平衡和后处理未控制，不能据此报 Delta-E 或绝对偏色量。
照片红帽并非纯 (255,0,0)，本来含高光和浅红，不能把所有白点直接删除。

## 下一阶段 TODO（用户继续后再执行）

- [ ] 确认设备已正常回主页；读取本文件、Git 状态，固定 21b0370 基准及配置。
- [ ] 建立可重复拍摄条件：均匀漫射光、固定角度/距离/曝光/白平衡，保留白边；
      将色块图与人像作为配对测试，不凭一张照片给物理调色板定值。
- [ ] 优先核实标称调色板与实际颜料/混色的差异。当前目标投影与误差扩散
      使用标称 sRGB，不是实测物理混色模型；原生触发 RGB 与测量色值应分离。
      如具备仪器再建立绝对色度 profile；只有手机时明确限于相对、定性比较。
- [ ] 检查中间调/肤色映射与色相候选集合切换：是否引入灰黄、层次不连续，
      用连续渐变和实际像素分布佐证，不能先硬塞肤色/红色经验系数。
- [ ] 把“平均颜色偏差”和“网点空间纹理”分开评估。先验证目标混色比例，
      再讨论抖动纹理；不要同时改调色板、补偿、FS 参数，避免失去因果对照。
- [ ] 受控离线 A/B：肤色/红到粉/红到黑/中性渐变、人像帽纹头发、紫/洋红/青色。
      同时保住纯红无异常白点、灰阶无彩色污染、近中性色连续、照片细节。
- [ ] 旧版本遗留的分色区补偿仍在代码中。不要宣称已经彻底消除补偿；
      仅在有数据支持的新模型通过 A/B 后考虑退役，不整体翻回官方路径。
- [ ] 如要改运行算法，先 host 回归与内存检查，再独立构建、单图对比、实拍验收。
      不改页面/网络/存储/驱动波形；需要扩大范围先与用户确认。
- [ ] 性能单独测：先把每次照片准备耗时绑定到对应显示记录，避免被后续主页覆盖；
      同一张图同一配置多次采样后才能评价速度回归。没有依据不改屏幕波形。
- [ ] 确认新方案有效后，再决定是否固化为高效 LUT，以及是否提交/推送。

## 耗时口径（避免明天误读）

- IMG_9912 色块图：准备 0.996795 s；整次 local_photo 17.633131 s；
  refresh 调用 17.152280 s；工作区 14,496 字节。
- IMG_9914 人像：reference_photo 成功，整次 21.309969 s；
  refresh 调用 19.616249 s；没有单独保存本次照片准备时间。
- 人像后又有 home_full 记录（总 16.889675 s）。当前 pipeline 快照是
  `ui / 0.241975 s / 0 bytes`，**不是照片处理时间**。
- refresh/panel_us 已包含 prepare_us，不能相加；也不是独立 BUSY 波形计时。
- render success 不是对硬件 BUSY 完成的独立检测。
- 色块与人像不是同图 A/B，不能据 17.63 -> 21.31 s 判定算法变慢。

## 接续资料与命令

先读：`tools/color_lab/NATIVE_CALIBRATION_PLAN.md`、
`tools/color_lab/CALIBRATION_FIX_20260908.md`、`tools/color_lab/REFERENCE_PHOTO.md`。

```sh
git status --short
git log -2 --oneline
bash tools/color_lab/run_host_tests.sh
/Users/xudanyang/.nvm/versions/node/v25.8.0/bin/node tools/color_lab/test_preview_source.mjs
/Users/xudanyang/.espressif/python_env/idf5.5_py3.9_env/bin/python tools/color_lab/read_display_metrics_usb.py --port /dev/cu.usbmodem83401 --timeout 5
```

设备正常运行 USB VID:PID 为 303A:4002；下载模式为 303A:1001，序列号
44:1B:F6:C1:34:10。每次操作先确认端口身份，端口名不能假设永远不变。
当前端口 `/dev/cu.usbmodem83401`。下载后通常需松开 Boot、单按 Reset。

明天可直接说：“读取 TODO_COLOR_CALIBRATION.md，从 21b0370 基准继续。”
